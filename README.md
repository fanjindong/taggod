# 标签之神

<p align="center">
  <img src="assets/logo.png" alt="标签之神 Logo" width="220">
</p>

`标签之神` 是一个 Chrome 标签页管理插件，只解决两个高频问题：

1. 一键理顺满屏标签。
2. 快速找回想看的页面。

当标签栏挤满小图标、同一个网站散在不同位置、刚才看过的页面找不到时，打开它就够了。

## 使用界面

![标签之神使用页面标注](assets/插件使用页面标注.png)

## 一键理顺满屏标签

点击“整理当前窗口”，插件会把当前窗口里的页面按主域名归拢到一起，并尽量保持你已经习惯的标签顺序。

固定标签会保留在前面，同一网站的多个页面会靠在一起。整理后的标签栏更适合继续工作，也更容易判断哪些页面还能关掉。

默认快捷键：

| 系统 | 快捷键 |
| --- | --- |
| macOS | `Command+Shift+Y` |
| Windows 和 Linux | `Ctrl+Shift+Y` |

## 快速找回想看的页面

打开弹窗后，搜索框会自动聚焦。你可以直接输入页面标题、网址片段或主域名，例如 `github`、`docs`、`jira`。

不输入内容时，弹窗会显示最近使用页面；输入搜索词后，可以点击结果切换，也可以用方向键选择后按 `Enter` 跳转。

默认打开快捷键：

| 系统 | 快捷键 |
| --- | --- |
| macOS | `Command+Shift+L` |
| Windows 和 Linux | `Ctrl+Shift+L` |

如果快捷键和你已有的扩展冲突，可以在 Chrome 的 `chrome://extensions/shortcuts` 里调整。

## 安装方式

1. 下载或克隆本项目。
2. 打开 Chrome 的 `chrome://extensions`。
3. 开启右上角的“开发者模式”。
4. 点击“加载已解压的扩展程序”。
5. 选择本项目目录。
6. 把“标签之神”固定到浏览器工具栏。

## 权限与隐私

`标签之神` 只使用标签管理所需权限：

- `tabs`：读取、激活、移动、创建和关闭标签页。
- `tabGroups`：创建和更新 Chrome 原生标签分组。
- `storage`：在本地保存插件配置。

所有数据保存在本地 `chrome.storage.local`，当前版本不包含账号系统，也不会上传你的标签页数据。

## 开发与校验

本项目不需要构建步骤，源码可以直接作为 Chrome 插件加载。

运行本地校验：

```bash
node scripts/校验插件.js
```

也可以分别检查脚本语法：

```bash
node --check background.js
node --check popup.js
```

## 许可证

本项目基于 MIT 许可证开源，详见 [LICENSE](LICENSE)。
