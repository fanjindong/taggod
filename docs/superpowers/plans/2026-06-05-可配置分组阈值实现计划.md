# 可配置分组阈值实现计划

> **给自动化执行者：** 本计划用于按任务逐步实现。每个步骤使用复选框跟踪，完成后必须运行对应校验。

**目标：** 把创建 Chrome 原生标签分组的最少标签数从写死的 `2` 改为用户可配置，并在保存配置后立即重新梳理当前窗口分组。

**架构：** 继续使用 `chrome.storage.local` 中的 `tabgod.settings` 保存用户配置。后台统一负责配置归一化、分组阈值判断和当前窗口重新梳理，弹窗只负责输入、保存和状态展示。

**技术栈：** Manifest V3、Chrome `tabs` / `tabGroups` / `storage` API、原生 JavaScript、原生 CSS、本地 Node 校验脚本。

---

## 文件结构

- 修改 `background.js`：新增 `minTabsPerGroup` 默认配置、配置归一化、分组阈值判断、保存配置接口、当前窗口重新梳理逻辑，并让恢复工作集读取同一阈值。
- 修改 `popup.html`：新增“分组阈值”数字输入和保存按钮。
- 修改 `popup.css`：新增设置行样式，保持弹窗紧凑布局。
- 修改 `popup.js`：渲染阈值配置、绑定保存事件、调用后台 `update-settings`。
- 修改 `scripts/校验插件.js`：补充分组阈值和重新梳理行为校验。
- 修改 `README.md`：补充用户配置说明。

## 任务 1：后台配置与阈值判断

**文件：**

- 修改：`background.js`
- 校验：`scripts/校验插件.js`

- [x] **步骤 1：给默认配置增加阈值字段**

在 `DEFAULT_SETTINGS` 中加入：

```js
  // 默认至少两个同主域名标签才建组，原因是单标签分组通常只会增加标签栏噪音。
  minTabsPerGroup: 2,
```

- [x] **步骤 2：新增阈值归一化函数**

在 `getGroupColor` 后新增：

```js
function normalizeMinTabsPerGroup(value) {
  const numericValue = typeof value === 'number' ? value : Number.NaN;

  if (!Number.isInteger(numericValue) || numericValue < 1) {
    // 阈值必须至少为 1，原因是 0 或负数会让“至少几个标签才分组”的语义失效。
    return DEFAULT_SETTINGS.minTabsPerGroup;
  }

  return numericValue;
}
```

- [x] **步骤 3：在 `normalizeSettings` 中接入阈值归一化**

在 `priorityGroups` 兜底之后加入：

```js
  normalized.minTabsPerGroup = normalizeMinTabsPerGroup(normalized.minTabsPerGroup);
```

- [x] **步骤 4：新增统一判断函数**

在 `isPriorityGroup` 前新增：

```js
function shouldCreateNativeGroup(tabIds, settings) {
  const normalizedSettings = normalizeSettings(settings);

  // 所有建组入口统一走这里，避免整理和恢复使用不同阈值。
  return Array.isArray(tabIds) && tabIds.length >= normalizedSettings.minTabsPerGroup;
}
```

- [x] **步骤 5：让一键整理复用重新梳理函数**

把 `organizeTabs()` 中排序后的直接建组逻辑替换为：

```js
  const reconcileResult = await reconcileCurrentWindowGroups(settings);

  return {
    organizedCount: tabs.length,
    groupCount: reconcileResult.groupedCount
  };
```

这样一键整理也会取消不满足阈值的旧分组，原因是整理结果应该和当前用户配置保持一致。

## 任务 2：配置保存后重新梳理当前窗口

**文件：**

- 修改：`background.js`
- 校验：`scripts/校验插件.js`

- [x] **步骤 1：新增当前窗口分组重新梳理函数**

在 `organizeTabs()` 后新增：

```js
async function reconcileCurrentWindowGroups(settings) {
  const normalizedSettings = normalizeSettings(settings);
  const tabs = await queryCurrentWindowTabs();
  const groups = new Map();

  tabs.forEach((tab) => {
    if (tab.pinned || typeof tab.id !== 'number') {
      return;
    }

    const groupKey = getDomainKey(tab.url || '');
    const groupTabs = groups.get(groupKey) || [];
    groupTabs.push(tab);
    groups.set(groupKey, groupTabs);
  });

  let groupedCount = 0;
  let ungroupedTabCount = 0;

  for (const [groupKey, groupTabs] of groups.entries()) {
    const tabIds = groupTabs.map((tab) => tab.id);

    if (!shouldCreateNativeGroup(tabIds, normalizedSettings)) {
      const groupedTabIds = groupTabs
        .filter((tab) => typeof tab.groupId === 'number' && tab.groupId >= 0)
        .map((tab) => tab.id);

      if (groupedTabIds.length > 0) {
        // 配置调高后，旧原生分组不再满足阈值，必须取消才能让当前页面状态和配置一致。
        const ungroupedCount = await chrome.tabs.ungroup(groupedTabIds)
          .then(() => groupedTabIds.length)
          .catch(() => 0);
        ungroupedTabCount += ungroupedCount;
      }

      continue;
    }

    const groupId = await chrome.tabs.group({ tabIds }).catch(() => null);

    if (typeof groupId === 'number') {
      await chrome.tabGroups.update(groupId, {
        title: groupKey,
        color: getGroupColor(groupedCount)
      });
      groupedCount += 1;
    }
  }

  return {
    groupedCount,
    ungroupedTabCount
  };
}
```

- [x] **步骤 2：新增设置更新函数**

在 `togglePriorityGroup` 前新增：

```js
async function updateSettings(partialSettings) {
  const stored = await chrome.storage.local.get([STORAGE_KEYS.settings]);
  const settings = normalizeSettings(Object.assign(
    {},
    stored[STORAGE_KEYS.settings] || {},
    partialSettings || {}
  ));

  await chrome.storage.local.set({ [STORAGE_KEYS.settings]: settings });
  const reconcileResult = await reconcileCurrentWindowGroups(settings);

  return Object.assign({
    settings
  }, reconcileResult);
}
```

- [x] **步骤 3：接入消息处理**

在 `handleMessage(message)` 中加入：

```js
  if (action === 'update-settings') {
    return updateSettings(message.settings);
  }
```

## 任务 3：恢复工作集使用同一阈值

**文件：**

- 修改：`background.js`

- [x] **步骤 1：让恢复流程读取设置**

把 `restoreSession(sessionId, options = {})` 开头的存储读取改为：

```js
  const stored = await chrome.storage.local.get([STORAGE_KEYS.sessions, STORAGE_KEYS.settings]);
  const sessions = sortWorkspaces(stored[STORAGE_KEYS.sessions] || []);
  const settings = normalizeSettings(stored[STORAGE_KEYS.settings]);
```

- [x] **步骤 2：把设置传入恢复建组函数**

把：

```js
  await regroupRestoredTabs(createdTabs);
```

改为：

```js
  await regroupRestoredTabs(createdTabs, settings);
```

- [x] **步骤 3：更新恢复建组函数签名和判断**

把 `regroupRestoredTabs(tabs)` 改为 `regroupRestoredTabs(tabs, settings = DEFAULT_SETTINGS)`，并把写死判断替换为：

```js
    if (!shouldCreateNativeGroup(tabIds, settings)) {
      // 恢复工作集时沿用用户阈值，避免恢复后出现用户刚刚选择隐藏的低数量分组。
      continue;
    }
```

## 任务 4：弹窗配置入口

**文件：**

- 修改：`popup.html`
- 修改：`popup.css`
- 修改：`popup.js`

- [x] **步骤 1：新增弹窗结构**

在 `action-grid` 后加入：

```html
      <section class="settings-strip" aria-label="整理设置">
        <label class="setting-field" for="minTabsPerGroupInput">
          <span>分组阈值</span>
          <input id="minTabsPerGroupInput" type="number" min="1" step="1" inputmode="numeric">
        </label>
        <span class="setting-unit">个标签</span>
        <button id="saveGroupThresholdButton" class="compact-button" type="button">保存</button>
      </section>
```

- [x] **步骤 2：新增样式**

在 `.action-grid` 样式后加入：

```css
.settings-strip {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto auto;
  align-items: end;
  gap: 8px;
  margin-top: 12px;
}

.setting-field {
  display: grid;
  gap: 4px;
  min-width: 0;
  font-size: 12px;
}

.setting-field input {
  width: 100%;
  height: 32px;
  border: 1px solid var(--line);
  border-radius: 8px;
  padding: 0 10px;
  font-size: 13px;
}

.setting-unit {
  padding-bottom: 8px;
  color: var(--muted);
  font-size: 12px;
}
```

- [x] **步骤 3：初始化前台设置状态**

把 `state.settings` 改为：

```js
  settings: {
    minTabsPerGroup: 2,
    priorityGroups: []
  },
```

- [x] **步骤 4：绑定保存按钮**

在 `bindEvents()` 中加入：

```js
  document.getElementById('saveGroupThresholdButton').addEventListener('click', saveGroupThreshold);
```

- [x] **步骤 5：渲染设置值**

在 `render()` 中调用 `renderSettings()`，并新增：

```js
function renderSettings() {
  document.getElementById('minTabsPerGroupInput').value = state.settings.minTabsPerGroup || 2;
}
```

- [x] **步骤 6：新增保存函数**

在 `saveWorkspace()` 前新增：

```js
async function saveGroupThreshold() {
  const input = document.getElementById('minTabsPerGroupInput');
  const minTabsPerGroup = Number(input.value);

  if (!Number.isInteger(minTabsPerGroup) || minTabsPerGroup < 1) {
    setStatus('分组阈值必须是不小于 1 的整数');
    return;
  }

  setBusy(true);

  try {
    const result = await sendMessage('update-settings', {
      settings: { minTabsPerGroup }
    });
    state.settings = result.settings || state.settings;
    setStatus(`已保存分组阈值：至少 ${state.settings.minTabsPerGroup} 个标签，已重新梳理当前窗口分组`);
    await loadState({ keepStatus: true });
  } catch (error) {
    setStatus(error.message || '保存分组阈值失败');
  } finally {
    setBusy(false);
  }
}
```

## 任务 5：校验与文档

**文件：**

- 修改：`scripts/校验插件.js`
- 修改：`README.md`

- [x] **步骤 1：补充后台函数校验**

在主域名断言后加入：

```js
assert.strictEqual(backgroundSandbox.normalizeSettings({}).minTabsPerGroup, 2);
assert.strictEqual(backgroundSandbox.normalizeSettings({ minTabsPerGroup: 3 }).minTabsPerGroup, 3);
assert.strictEqual(backgroundSandbox.normalizeSettings({ minTabsPerGroup: 0 }).minTabsPerGroup, 2);
assert.strictEqual(backgroundSandbox.normalizeSettings({ minTabsPerGroup: 1 }).minTabsPerGroup, 1);
assert.strictEqual(backgroundSandbox.normalizeSettings({ minTabsPerGroup: 2.5 }).minTabsPerGroup, 2);
assert.strictEqual(backgroundSandbox.normalizeSettings({ minTabsPerGroup: '3' }).minTabsPerGroup, 2);
assert.strictEqual(backgroundSandbox.shouldCreateNativeGroup([1, 2], { minTabsPerGroup: 2 }), true);
assert.strictEqual(backgroundSandbox.shouldCreateNativeGroup([1, 2], { minTabsPerGroup: 3 }), false);
assert.strictEqual(backgroundSandbox.shouldCreateNativeGroup([1], { minTabsPerGroup: 1 }), true);
```

- [x] **步骤 2：补充重新梳理校验**

在后台函数校验后加入：

```js
async function runAsyncChecks() {
  const groupOperations = {
    grouped: [],
    ungrouped: [],
    updated: []
  };

  backgroundSandbox.queryCurrentWindowTabs = async () => [
    { id: 21, url: 'https://mail.google.com/inbox', pinned: false, groupId: 1 },
    { id: 22, url: 'https://docs.google.com/document', pinned: false, groupId: 1 },
    { id: 23, url: 'https://calendar.google.com/calendar', pinned: false, groupId: 1 },
    { id: 31, url: 'https://a.example.com', pinned: false, groupId: 2 },
    { id: 41, url: 'https://solo.test.com', pinned: false, groupId: 3 },
    { id: 51, url: 'https://pinned.test.com', pinned: true, groupId: 4 }
  ];
  backgroundSandbox.chrome.tabs = {
    group: async ({ tabIds }) => {
      groupOperations.grouped.push(Array.from(tabIds));
      return groupOperations.grouped.length;
    },
    ungroup: async (tabIds) => {
      groupOperations.ungrouped.push(Array.from(tabIds));
    }
  };
  backgroundSandbox.chrome.tabGroups = {
    update: async (groupId, options) => {
      groupOperations.updated.push({ groupId, options });
    }
  };

  await backgroundSandbox.reconcileCurrentWindowGroups({ minTabsPerGroup: 3 });
  assert.deepStrictEqual(groupOperations.grouped, [[21, 22, 23]]);
  assert.deepStrictEqual(groupOperations.ungrouped, [[31], [41]]);
  assert.strictEqual(groupOperations.updated[0].options.title, 'google.com');
}

runAsyncChecks()
  .then(() => {
    console.log('插件文件校验通过');
  })
  .catch((error) => {
    console.error(error);
    process.exitCode = 1;
  });
```

- [x] **步骤 3：更新 README**

在“功能特性”中补充：

```markdown
- 分组阈值可配置，保存后会立即重新梳理当前窗口分组。
```

在“整理规则”后补充：

```markdown
## 分组阈值

弹窗中的“分组阈值”用于控制同一主域名至少有多少个标签才创建 Chrome 原生分组。默认值为 `2`，原因是单个标签单独建组通常会增加标签栏噪音。

保存阈值后，插件会立即重新梳理当前窗口分组：不满足新阈值的旧分组会被取消，满足新阈值的主域名会被创建或保留为分组。
```

- [x] **步骤 4：运行校验**

依次运行：

```bash
node --check background.js
node --check popup.js
node scripts/校验插件.js
```

预期结果：

```text
插件文件校验通过
```
