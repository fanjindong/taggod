# 搜索优先弹窗重构实现计划

> **给自动化执行者：** 必须逐项执行本计划。实现时使用 `superpowers:subagent-driven-development` 或 `superpowers:executing-plans` 按任务推进；每个步骤使用复选框语法跟踪状态。所有新增注释、文档和提交信息必须使用中文。

**目标：** 将弹窗主路径重构为“快速搜索并切换已打开标签”，默认展示跨窗口最近使用页面，并把整理、去重、工作集和设置迁移到二级入口。

**架构：** 后台继续负责 Chrome 标签和窗口 API 调用，新增跨窗口标签快照、最近使用兜底记录和跨窗口激活能力。弹窗负责搜索词、结果排序、选中项、点击切换和键盘切换，避免输入时频繁请求后台。现有整理、去重、工作集和设置能力不重写，只迁移入口。

**技术栈：** Manifest V3、原生 JavaScript、Chrome Tabs API、Chrome Windows API、Chrome Storage API、原生 CSS、Node.js、`vm`、`assert`。

---

## 文件结构

- 修改 `background.js`：新增跨窗口标签查询、窗口标识、最近使用兜底记录、跨窗口激活接口，并保持现有整理和工作集逻辑。
- 修改 `popup.html`：重排弹窗结构，首屏改为搜索框、状态行、结果列表、整理和更多入口。
- 修改 `popup.css`：新增快速切换列表、选中态、状态行、更多工具折叠区样式，保留现有管理区样式。
- 修改 `popup.js`：新增搜索排序、最近使用默认态、点击切换、上下键选择、回车切换和更多工具展开逻辑。
- 修改 `scripts/校验插件.js`：为跨窗口快照、最近使用排序、搜索排序和键盘边界补充纯函数校验。
- 修改 `README.md`：同步说明“快速切换”主路径、跨窗口搜索和最近使用默认态。

---

## 任务 1：补充后台跨窗口快照纯函数

**文件：**

- 修改：`background.js`
- 修改：`scripts/校验插件.js`

- [ ] **步骤 1：先在校验脚本中加入跨窗口快照失败用例。**

在 `scripts/校验插件.js` 的分组显示名断言后加入：

```js
const windowOrderMap = backgroundSandbox.buildWindowOrderMap([
  { id: 1, windowId: 20 },
  { id: 2, windowId: 10 },
  { id: 3, windowId: 30 },
  { id: 4, windowId: 20 }
], 20);

assert.strictEqual(windowOrderMap.get(20), 1);
assert.strictEqual(windowOrderMap.get(10), 2);
assert.strictEqual(windowOrderMap.get(30), 3);
assert.strictEqual(backgroundSandbox.getWindowLabel(20, 20, windowOrderMap), '当前窗口');
assert.strictEqual(backgroundSandbox.getWindowLabel(10, 20, windowOrderMap), '其他窗口');
assert.strictEqual(backgroundSandbox.getWindowLabel(999, 20, windowOrderMap), '其他窗口');

const searchSnapshots = backgroundSandbox.buildSearchTabSnapshots([
  {
    id: 101,
    title: '飞书需求文档',
    url: 'https://docs.example.com/a',
    active: false,
    pinned: false,
    index: 2,
    windowId: 20,
    lastAccessed: 1780645200000
  },
  {
    id: 102,
    title: 'GitHub PR',
    url: 'https://github.com/example/repo/pull/1',
    active: true,
    pinned: false,
    index: 1,
    windowId: 10
  }
], {
  currentWindowId: 20,
  recentAccessMap: {
    102: 1780645300000
  }
});

assert.strictEqual(searchSnapshots[0].groupKey, 'example.com');
assert.strictEqual(searchSnapshots[0].groupTitle, 'example');
assert.strictEqual(searchSnapshots[0].isCurrentWindow, true);
assert.strictEqual(searchSnapshots[0].windowLabel, '当前窗口');
assert.strictEqual(searchSnapshots[0].lastAccessedAt, 1780645200000);
assert.strictEqual(searchSnapshots[1].groupKey, 'github.com');
assert.strictEqual(searchSnapshots[1].groupTitle, 'github');
assert.strictEqual(searchSnapshots[1].isCurrentWindow, false);
assert.strictEqual(searchSnapshots[1].windowLabel, '其他窗口');
assert.strictEqual(searchSnapshots[1].lastAccessedAt, 1780645300000);
```

- [ ] **步骤 2：运行校验，确认新增用例失败。**

运行：

```bash
node scripts/校验插件.js
```

预期结果：失败信息包含 `buildWindowOrderMap is not a function` 或同类函数未定义错误。

- [ ] **步骤 3：在后台新增窗口和最近使用快照函数。**

在 `background.js` 的 `buildGroupTitleMap` 后加入：

```js
function buildWindowOrderMap(tabs, currentWindowId) {
  const currentId = Number.isInteger(currentWindowId) ? currentWindowId : null;
  const windowIds = Array.from(new Set((Array.isArray(tabs) ? tabs : [])
    .map((tab) => tab.windowId)
    .filter((windowId) => Number.isInteger(windowId))));
  const orderedIds = [];

  if (currentId !== null && windowIds.includes(currentId)) {
    orderedIds.push(currentId);
  }

  windowIds
    .filter((windowId) => windowId !== currentId)
    .sort((left, right) => left - right)
    .forEach((windowId) => orderedIds.push(windowId));

  return new Map(orderedIds.map((windowId, index) => [windowId, index + 1]));
}

function getWindowLabel(windowId, currentWindowId, windowOrderMap) {
  if (Number.isInteger(windowId) && windowId === currentWindowId) {
    return '当前窗口';
  }

  // 非当前窗口只提示来源差异，不暴露用户难以理解的内部编号。
  return '其他窗口';
}

function getTabLastAccessedAt(tab, recentAccessMap = {}) {
  if (Number.isFinite(tab && tab.lastAccessed)) {
    return tab.lastAccessed;
  }

  const recentAccessedAt = recentAccessMap && recentAccessMap[String(tab && tab.id)];

  if (Number.isFinite(recentAccessedAt)) {
    // 旧版本浏览器可能没有 lastAccessed，本地激活记录用于保持最近使用排序可用。
    return recentAccessedAt;
  }

  return 0;
}

function buildSearchTabSnapshots(tabs, context = {}) {
  const safeTabs = Array.isArray(tabs) ? tabs : [];
  const currentWindowId = Number.isInteger(context.currentWindowId) ? context.currentWindowId : null;
  const recentAccessMap = context.recentAccessMap || {};
  const titleMap = buildGroupTitleMap(safeTabs.map((tab) => getDomainKey(tab.url || '')));
  const windowOrderMap = buildWindowOrderMap(safeTabs, currentWindowId);

  return safeTabs.map((tab) => {
    const groupKey = getDomainKey(tab.url || '');

    return Object.assign({}, buildTabSnapshot(tab), {
      windowId: Number.isInteger(tab.windowId) ? tab.windowId : null,
      groupTitle: titleMap.get(groupKey) || groupKey,
      isCurrentWindow: Number.isInteger(tab.windowId) && tab.windowId === currentWindowId,
      windowLabel: getWindowLabel(tab.windowId, currentWindowId, windowOrderMap),
      lastAccessedAt: getTabLastAccessedAt(tab, recentAccessMap)
    });
  });
}
```

- [ ] **步骤 4：再次运行校验，确认跨窗口快照用例通过。**

运行：

```bash
node scripts/校验插件.js
```

预期结果：输出 `插件文件校验通过`。

---

## 任务 2：新增最近使用兜底记录和跨窗口状态接口

**文件：**

- 修改：`background.js`
- 修改：`scripts/校验插件.js`

- [ ] **步骤 1：在校验脚本中加入最近使用记录归一化用例。**

在任务 1 新增断言后继续加入：

```js
const normalizedRecentAccess = backgroundSandbox.normalizeRecentAccessMap({
  101: 1780645000000,
  102: '不是时间',
  abc: 1780645100000
});

assert.deepStrictEqual(normalizedRecentAccess, {
  101: 1780645000000
});
```

- [ ] **步骤 2：运行校验，确认新增用例失败。**

运行：

```bash
node scripts/校验插件.js
```

预期结果：失败信息包含 `normalizeRecentAccessMap is not a function`。

- [ ] **步骤 3：扩展后台存储键和最近使用归一化函数。**

把 `background.js` 顶部的 `STORAGE_KEYS` 改为：

```js
const STORAGE_KEYS = {
  sessions: 'tabgod.sessions',
  settings: 'tabgod.settings',
  recentAccess: 'tabgod.recentAccess'
};
```

在 `DEFAULT_SETTINGS` 后加入：

```js
// 只保留最近 300 个标签激活记录，原因是该记录只用于排序兜底，过多历史会浪费本地存储。
const RECENT_ACCESS_LIMIT = 300;
```

在 `normalizeWorkspace` 后加入：

```js
function normalizeRecentAccessMap(value) {
  const entries = Object.entries(value || {})
    .filter(([tabId, accessedAt]) => Number.isInteger(Number(tabId)) && Number.isFinite(accessedAt))
    .sort((left, right) => right[1] - left[1])
    .slice(0, RECENT_ACCESS_LIMIT);

  return Object.fromEntries(entries);
}
```

- [ ] **步骤 4：新增最近使用写入和清理函数。**

在 `normalizeRecentAccessMap` 后加入：

```js
async function recordRecentTabAccess(tabId) {
  if (!Number.isInteger(tabId)) {
    return;
  }

  const stored = await chrome.storage.local.get([STORAGE_KEYS.recentAccess]);
  const recentAccess = normalizeRecentAccessMap(stored[STORAGE_KEYS.recentAccess]);
  recentAccess[String(tabId)] = Date.now();

  await chrome.storage.local.set({
    [STORAGE_KEYS.recentAccess]: normalizeRecentAccessMap(recentAccess)
  });
}

async function removeRecentTabAccess(tabId) {
  if (!Number.isInteger(tabId)) {
    return;
  }

  const stored = await chrome.storage.local.get([STORAGE_KEYS.recentAccess]);
  const recentAccess = normalizeRecentAccessMap(stored[STORAGE_KEYS.recentAccess]);

  if (!Object.prototype.hasOwnProperty.call(recentAccess, String(tabId))) {
    return;
  }

  delete recentAccess[String(tabId)];

  await chrome.storage.local.set({
    [STORAGE_KEYS.recentAccess]: recentAccess
  });
}
```

- [ ] **步骤 5：新增跨窗口查询函数并接入现有状态接口。**

在 `queryCurrentWindowTabs` 后加入：

```js
async function queryAllWindowTabs() {
  return chrome.tabs.query({});
}

async function getCurrentWindowId() {
  const tabs = await chrome.tabs.query({ active: true, currentWindow: true });
  const activeTab = tabs[0];

  return activeTab && Number.isInteger(activeTab.windowId) ? activeTab.windowId : null;
}
```

把 `getPopupState()` 替换为：

```js
async function getPopupState() {
  const currentTabs = await queryCurrentWindowTabs();
  const allTabs = await queryAllWindowTabs();
  const currentWindowId = await getCurrentWindowId();
  const stored = await chrome.storage.local.get([
    STORAGE_KEYS.sessions,
    STORAGE_KEYS.settings,
    STORAGE_KEYS.recentAccess
  ]);
  const settings = normalizeSettings(stored[STORAGE_KEYS.settings]);
  const recentAccessMap = normalizeRecentAccessMap(stored[STORAGE_KEYS.recentAccess]);
  const searchTabs = buildSearchTabSnapshots(allTabs, {
    currentWindowId,
    recentAccessMap
  });
  const windowCount = new Set(searchTabs
    .map((tab) => tab.windowId)
    .filter((windowId) => Number.isInteger(windowId))).size;

  return {
    tabs: searchTabs,
    groups: buildGroupSummaries(currentTabs, settings),
    overview: Object.assign({}, buildOverview(currentTabs), {
      allTabCount: searchTabs.length,
      windowCount
    }),
    sessions: sortWorkspaces(stored[STORAGE_KEYS.sessions] || []),
    settings,
    currentWindowId
  };
}
```

- [ ] **步骤 6：监听标签激活和关闭事件。**

在 `chrome.commands.onCommand.addListener` 前加入：

```js
chrome.tabs.onActivated.addListener((activeInfo) => {
  // 事件记录只作为 lastAccessed 缺失时的排序兜底，失败不影响标签切换主流程。
  recordRecentTabAccess(activeInfo.tabId).catch(() => undefined);
});

chrome.tabs.onRemoved.addListener((tabId) => {
  // 标签关闭后清理本地记录，避免已关闭标签长期占用最近使用存储。
  removeRecentTabAccess(tabId).catch(() => undefined);
});
```

- [ ] **步骤 7：运行语法检查和插件校验。**

运行：

```bash
node --check background.js
node scripts/校验插件.js
```

预期结果：两个命令都成功，校验脚本输出 `插件文件校验通过`。

---

## 任务 3：实现跨窗口激活目标标签

**文件：**

- 修改：`background.js`
- 修改：`scripts/校验插件.js`

- [ ] **步骤 1：在异步校验中加入跨窗口激活用例。**

在 `scripts/校验插件.js` 的 `runAsyncChecks()` 内，现有 `reconcileCurrentWindowGroups` 断言之后加入：

```js
  const activationOperations = {
    focusedWindowIds: [],
    activatedTabIds: []
  };

  backgroundSandbox.chrome.tabs.get = async (tabId) => {
    assert.strictEqual(tabId, 88);
    return {
      id: 88,
      windowId: 66
    };
  };
  backgroundSandbox.chrome.windows = {
    update: async (windowId, updateInfo) => {
      activationOperations.focusedWindowIds.push(windowId);
      assert.deepStrictEqual(updateInfo, { focused: true });
    }
  };
  backgroundSandbox.chrome.tabs.update = async (tabId, updateInfo) => {
    activationOperations.activatedTabIds.push(tabId);
    assert.deepStrictEqual(updateInfo, { active: true });
  };

  const activationResult = await backgroundSandbox.activateTabAcrossWindows(88);
  assert.deepStrictEqual(activationOperations.focusedWindowIds, [66]);
  assert.deepStrictEqual(activationOperations.activatedTabIds, [88]);
  assert.deepStrictEqual(activationResult, {
    activated: true,
    windowId: 66
  });
```

- [ ] **步骤 2：运行校验，确认新增用例失败。**

运行：

```bash
node scripts/校验插件.js
```

预期结果：失败信息包含 `activateTabAcrossWindows is not a function`。

- [ ] **步骤 3：新增跨窗口激活函数。**

在 `closeSavedTabs` 前加入：

```js
async function activateTabAcrossWindows(tabId) {
  if (!Number.isInteger(tabId)) {
    throw new Error('目标标签无效');
  }

  const tab = await chrome.tabs.get(tabId);

  if (!tab || !Number.isInteger(tab.id)) {
    throw new Error('目标标签可能已关闭');
  }

  if (Number.isInteger(tab.windowId)) {
    // 跨窗口切换必须先聚焦窗口，否则只激活标签会让用户看不到目标页面。
    await chrome.windows.update(tab.windowId, { focused: true });
  }

  await chrome.tabs.update(tab.id, { active: true });
  await recordRecentTabAccess(tab.id).catch(() => undefined);

  return {
    activated: true,
    windowId: Number.isInteger(tab.windowId) ? tab.windowId : null
  };
}
```

- [ ] **步骤 4：把消息入口改为跨窗口激活。**

在 `handleMessage(message)` 中把 `activate-tab` 分支替换为：

```js
  if (action === 'activate-tab') {
    return activateTabAcrossWindows(message.tabId);
  }
```

- [ ] **步骤 5：运行语法检查和插件校验。**

运行：

```bash
node --check background.js
node scripts/校验插件.js
```

预期结果：两个命令都成功，校验脚本输出 `插件文件校验通过`。

---

## 任务 4：新增弹窗搜索排序纯函数

**文件：**

- 修改：`popup.js`
- 修改：`scripts/校验插件.js`

- [ ] **步骤 1：让校验脚本也沙箱执行弹窗脚本。**

在 `scripts/校验插件.js` 中读取 `popupHtml` 前加入：

```js
const popupPath = path.join(rootDir, 'popup.js');
const popupSandbox = {
  console,
  document: {
    addEventListener() {},
    getElementById() {
      return {
        addEventListener() {},
        classList: {
          toggle() {}
        },
        querySelectorAll() {
          return [];
        },
        appendChild() {},
        innerHTML: '',
        textContent: '',
        value: '',
        hidden: false
      };
    },
    querySelectorAll() {
      return [];
    },
    createElement() {
      return {
        className: '',
        dataset: {},
        classList: {
          toggle() {}
        },
        addEventListener() {},
        appendChild() {},
        querySelectorAll() {
          return [];
        },
        set innerHTML(value) {
          this.html = value;
        },
        get innerHTML() {
          return this.html || '';
        }
      };
    }
  },
  chrome: {
    runtime: {
      sendMessage() {
        return Promise.resolve({ ok: true, payload: {} });
      }
    }
  },
  window: {
    prompt() {
      return null;
    },
    confirm() {
      return false;
    }
  },
  Intl,
  Date,
  Number,
  String,
  Array,
  Set,
  Map
};

vm.createContext(popupSandbox);
vm.runInContext(fs.readFileSync(popupPath, 'utf8'), popupSandbox, { filename: 'popup.js' });
```

- [ ] **步骤 2：加入搜索排序失败用例。**

在弹窗沙箱代码后加入：

```js
const rankedRecentTabs = popupSandbox.getVisibleTabsFromState({
  query: '',
  tabs: [
    { id: 1, title: '旧页面', url: 'https://old.example.com', groupKey: 'example.com', groupTitle: 'example', index: 0, isCurrentWindow: true, lastAccessedAt: 10 },
    { id: 2, title: '新页面', url: 'https://new.example.com', groupKey: 'example.com', groupTitle: 'example', index: 1, isCurrentWindow: false, lastAccessedAt: 30 },
    { id: 3, title: '无时间页面', url: 'https://none.example.com', groupKey: 'example.com', groupTitle: 'example', index: 2, isCurrentWindow: true, lastAccessedAt: 0 }
  ]
});
assert.deepStrictEqual(Array.from(rankedRecentTabs, (tab) => tab.id), [2, 1, 3]);

const rankedSearchTabs = popupSandbox.getVisibleTabsFromState({
  query: 'github',
  tabs: [
    { id: 10, title: '普通文章', url: 'https://example.com/github-guide', groupKey: 'example.com', groupTitle: 'example', index: 0, isCurrentWindow: true, lastAccessedAt: 100 },
    { id: 11, title: 'GitHub PR', url: 'https://github.com/acme/repo/pull/1', groupKey: 'github.com', groupTitle: 'github', index: 1, isCurrentWindow: false, lastAccessedAt: 50 },
    { id: 12, title: 'GitHub 首页', url: 'https://github.com', groupKey: 'github.com', groupTitle: 'github', index: 2, isCurrentWindow: true, lastAccessedAt: 30 }
  ]
});
assert.deepStrictEqual(Array.from(rankedSearchTabs, (tab) => tab.id), [11, 12, 10]);

assert.strictEqual(popupSandbox.clampSelectedIndex(-1, 3), 2);
assert.strictEqual(popupSandbox.clampSelectedIndex(3, 3), 0);
assert.strictEqual(popupSandbox.clampSelectedIndex(1, 3), 1);
assert.strictEqual(popupSandbox.clampSelectedIndex(0, 0), -1);
```

- [ ] **步骤 3：运行校验，确认弹窗纯函数缺失。**

运行：

```bash
node scripts/校验插件.js
```

预期结果：失败信息包含 `getVisibleTabsFromState is not a function`。

- [ ] **步骤 4：在弹窗状态中新增选中索引。**

把 `popup.js` 顶部 `state` 中的 `query` 字段改为：

```js
  query: '',
  selectedIndex: 0,
  moreToolsVisible: false
```

- [ ] **步骤 5：新增搜索排序纯函数。**

在 `getVisibleTabs()` 前加入：

```js
function normalizeSearchText(value) {
  return String(value || '').trim().toLowerCase();
}

function getTabSearchText(tab) {
  return [
    tab.title,
    tab.url,
    tab.groupKey,
    tab.groupTitle
  ].map(normalizeSearchText).join(' ');
}

function getSearchMatchScore(tab, query) {
  const normalizedQuery = normalizeSearchText(query);

  if (!normalizedQuery) {
    return 0;
  }

  const title = normalizeSearchText(tab.title);
  const groupKey = normalizeSearchText(tab.groupKey);
  const groupTitle = normalizeSearchText(tab.groupTitle);
  const url = normalizeSearchText(tab.url);

  if (title === normalizedQuery) {
    return 400;
  }

  if (title.includes(normalizedQuery)) {
    return 300;
  }

  if (groupTitle.includes(normalizedQuery) || groupKey.includes(normalizedQuery)) {
    return 220;
  }

  if (url.includes(normalizedQuery)) {
    return 100;
  }

  return 0;
}

function compareRecentTabs(left, right) {
  const leftAccessedAt = Number(left.lastAccessedAt) || 0;
  const rightAccessedAt = Number(right.lastAccessedAt) || 0;

  if (leftAccessedAt !== rightAccessedAt) {
    return rightAccessedAt - leftAccessedAt;
  }

  if (left.isCurrentWindow !== right.isCurrentWindow) {
    return left.isCurrentWindow ? -1 : 1;
  }

  return (left.index || 0) - (right.index || 0);
}

function compareSearchTabs(left, right, query) {
  const leftScore = getSearchMatchScore(left, query);
  const rightScore = getSearchMatchScore(right, query);

  if (leftScore !== rightScore) {
    return rightScore - leftScore;
  }

  const recentCompare = compareRecentTabs(left, right);

  if (recentCompare !== 0) {
    return recentCompare;
  }

  return (left.id || 0) - (right.id || 0);
}

function getVisibleTabsFromState(sourceState) {
  const query = normalizeSearchText(sourceState.query);
  const tabs = Array.isArray(sourceState.tabs) ? sourceState.tabs : [];

  if (!query) {
    return [...tabs].sort(compareRecentTabs);
  }

  return tabs
    .filter((tab) => getSearchMatchScore(tab, query) > 0 || getTabSearchText(tab).includes(query))
    .sort((left, right) => compareSearchTabs(left, right, query));
}

function clampSelectedIndex(index, total) {
  if (total <= 0) {
    return -1;
  }

  if (index < 0) {
    return total - 1;
  }

  if (index >= total) {
    return 0;
  }

  return index;
}
```

- [ ] **步骤 6：改造 `getVisibleTabs()` 使用新排序。**

把 `getVisibleTabs()` 替换为：

```js
function getVisibleTabs() {
  return getVisibleTabsFromState(state);
}
```

- [ ] **步骤 7：运行语法检查和插件校验。**

运行：

```bash
node --check popup.js
node scripts/校验插件.js
```

预期结果：两个命令都成功，校验脚本输出 `插件文件校验通过`。

---

## 任务 5：重排弹窗 HTML 为搜索优先结构

**文件：**

- 修改：`popup.html`
- 修改：`scripts/校验插件.js`

- [ ] **步骤 1：先补充弹窗结构校验。**

在 `scripts/校验插件.js` 的 `popupHtml` 断言区域加入：

```js
assert.ok(popupHtml.includes('快速切换'));
assert.ok(popupHtml.includes('searchResultList'));
assert.ok(popupHtml.includes('moreToolsButton'));
assert.ok(popupHtml.includes('moreToolsSection'));
assert.ok(popupHtml.includes('打开后默认展示最近使用页面'));
```

- [ ] **步骤 2：运行校验，确认结构文案缺失。**

运行：

```bash
node scripts/校验插件.js
```

预期结果：失败信息来自新增的 `popupHtml.includes` 断言。

- [ ] **步骤 3：重排 `popup.html` 主体结构。**

把 `<main class="popup-shell">` 内部替换为：

```html
      <header class="popup-header">
        <div>
          <h1>快速切换</h1>
          <p id="summaryText">打开后默认展示最近使用页面</p>
        </div>
      </header>

      <label class="search-box quick-search-box">
        <span>搜索已打开标签</span>
        <input id="searchInput" type="search" placeholder="输入标题、网址或主域名" autocomplete="off">
      </label>

      <section class="quick-status" aria-label="标签概览">
        <span id="quickStatusText">正在读取标签页</span>
        <span id="duplicateHintText"></span>
      </section>

      <section class="list-section quick-results-section" aria-label="快速切换结果">
        <div class="section-title">
          <h2 id="resultTitle">最近使用</h2>
          <span id="visibleCount">0 个结果</span>
        </div>
        <div id="searchResultList" class="tab-list quick-result-list"></div>
      </section>

      <section class="action-grid quick-action-grid" aria-label="常用操作">
        <button id="organizeButton" class="primary-action" type="button">整理当前窗口</button>
        <button id="moreToolsButton" type="button" aria-expanded="false" aria-controls="moreToolsSection">更多工具</button>
      </section>

      <section id="moreToolsSection" class="more-tools-section is-hidden" aria-label="更多工具" hidden>
        <section class="settings-strip" aria-label="整理设置">
          <label class="setting-field" for="minTabsPerGroupInput">
            <span>分组阈值</span>
            <input id="minTabsPerGroupInput" type="number" min="1" step="1" inputmode="numeric">
          </label>
          <span class="setting-unit">个标签</span>
          <button id="saveGroupThresholdButton" class="compact-button" type="button">保存</button>
        </section>

        <section id="duplicateReviewSection" class="list-section is-hidden" aria-label="重复标签确认" hidden>
          <div class="section-title">
            <h2>重复确认</h2>
            <span id="duplicateReviewCount">0 个可关闭</span>
          </div>
          <div id="duplicateReviewList" class="duplicate-review-list"></div>
          <div class="review-actions">
            <button id="closeSelectedDuplicatesButton" class="danger-button" type="button">关闭已勾选重复标签</button>
            <button id="cancelDuplicateReviewButton" type="button">取消</button>
          </div>
        </section>

        <section class="list-section">
          <div class="section-title">
            <h2>管理操作</h2>
          </div>
          <div class="tool-button-grid">
            <button id="scanDuplicatesButton" type="button">智能去重</button>
            <button id="saveWorkspaceButton" type="button">保存工作集</button>
          </div>
        </section>

        <section class="list-section">
          <div class="section-title">
            <h2>优先分组</h2>
          </div>
          <div id="groupList" class="group-list"></div>
        </section>

        <section class="list-section">
          <div class="section-title">
            <h2>工作集</h2>
          </div>
          <div id="sessionList" class="session-list"></div>
        </section>
      </section>

      <p id="statusText" class="status-text" role="status"></p>
```

- [ ] **步骤 4：运行 HTML 文案校验。**

运行：

```bash
node scripts/校验插件.js
```

预期结果：如果弹窗脚本尚未切换到 `searchResultList`，校验可能继续失败；HTML 相关断言不再失败。

---

## 任务 6：改造弹窗渲染和点击切换

**文件：**

- 修改：`popup.js`
- 修改：`scripts/校验插件.js`

- [ ] **步骤 1：修改状态加载后的默认选中项和搜索聚焦。**

在 `loadState(options = {})` 中 `render();` 前加入：

```js
    state.selectedIndex = clampSelectedIndex(state.selectedIndex, getVisibleTabsFromState(state).length);
```

在 `render();` 后加入：

```js
    focusSearchInput();
```

在 `renderSettings()` 前加入：

```js
function focusSearchInput() {
  const searchInput = document.getElementById('searchInput');

  if (searchInput && typeof searchInput.focus === 'function') {
    // 弹窗打开后的主任务是找标签，自动聚焦可以减少一次无意义点击。
    searchInput.focus();
  }
}
```

- [ ] **步骤 2：改造搜索输入事件。**

把 `bindEvents()` 中搜索框的 `input` 监听替换为：

```js
  document.getElementById('searchInput').addEventListener('input', (event) => {
    state.query = event.target.value.trim().toLowerCase();
    state.selectedIndex = 0;
    renderTabs();
  });
```

- [ ] **步骤 3：把结果容器切到 `searchResultList`。**

把 `renderTabs()` 函数开头替换为：

```js
function renderTabs() {
  const tabList = document.getElementById('searchResultList');
  const visibleTabs = getVisibleTabs();
  const selectedIndex = clampSelectedIndex(state.selectedIndex, visibleTabs.length);
  state.selectedIndex = selectedIndex;
  document.getElementById('visibleCount').textContent = `${visibleTabs.length} 个结果`;
  document.getElementById('resultTitle').textContent = state.query ? '搜索结果' : '最近使用';
  tabList.innerHTML = '';
```

- [ ] **步骤 4：把结果项改成可点击切换。**

把 `renderTabs()` 中 `visibleTabs.forEach((tab) => { ... })` 整段替换为：

```js
  visibleTabs.forEach((tab, index) => {
    const item = document.createElement('button');
    item.className = `tab-item quick-result-item${index === selectedIndex ? ' is-selected' : ''}`;
    item.type = 'button';
    item.dataset.action = 'activate';
    item.dataset.tabId = String(tab.id);
    const groupTitle = tab.groupTitle || tab.groupKey;
    const windowLabel = tab.windowLabel || (tab.isCurrentWindow ? '当前窗口' : '其他窗口');
    item.innerHTML = `
      <span class="quick-result-main">
        <span class="tab-title" title="${escapeHtml(tab.title)}">${escapeHtml(tab.title)}</span>
        <span class="tab-url" title="${escapeHtml(tab.groupKey)} · ${escapeHtml(tab.url)}">${escapeHtml(groupTitle)} · ${escapeHtml(tab.url)}</span>
      </span>
      <span class="quick-result-meta">${escapeHtml(windowLabel)}</span>
    `;
    item.addEventListener('click', () => activateSearchResult(tab.id));
    tabList.appendChild(item);
  });
```

- [ ] **步骤 5：新增点击切换函数。**

在 `closeSelectedDuplicates()` 前加入：

```js
async function activateSearchResult(tabId) {
  if (!Number.isInteger(tabId)) {
    setStatus('目标标签无效');
    return;
  }

  await runAction('activate-tab', { tabId });
}
```

- [ ] **步骤 6：删除旧结果按钮绑定。**

从 `renderTabs()` 中删除旧的：

```js
  tabList.querySelectorAll('button[data-action="activate"]').forEach((button) => {
    button.addEventListener('click', () => runAction('activate-tab', { tabId: Number(button.dataset.tabId) }));
  });

  tabList.querySelectorAll('button[data-action="close"]').forEach((button) => {
    button.addEventListener('click', () => runAction('close-tab', { tabId: Number(button.dataset.tabId) }));
  });
```

这个删除是必要的，原因是快速切换结果项本身就是按钮，嵌套旧按钮会让点击区域和键盘焦点混乱。关闭单个标签的能力首版不放在搜索结果首屏，避免误关。

- [ ] **步骤 7：更新状态行渲染。**

把 `renderOverview()` 替换为：

```js
function renderOverview() {
  const allTabCount = state.overview.allTabCount || state.overview.tabCount;
  const windowCount = state.overview.windowCount || 1;
  const duplicateCount = state.overview.duplicateCount || 0;

  document.getElementById('quickStatusText').textContent = `${allTabCount} 个标签 · ${windowCount} 个窗口`;
  document.getElementById('duplicateHintText').textContent = duplicateCount > 0 ? `${duplicateCount} 个重复` : '';
  document.getElementById('summaryText').textContent = state.query ? '搜索所有已打开标签' : '最近使用页面';
}
```

- [ ] **步骤 8：运行语法检查和校验。**

运行：

```bash
node --check popup.js
node scripts/校验插件.js
```

预期结果：两个命令都成功，校验脚本输出 `插件文件校验通过`。

---

## 任务 7：实现键盘选择和更多工具展开

**文件：**

- 修改：`popup.js`
- 修改：`scripts/校验插件.js`

- [ ] **步骤 1：新增键盘事件绑定。**

在 `bindEvents()` 的搜索框 `input` 监听后加入：

```js
  document.getElementById('searchInput').addEventListener('keydown', handleSearchKeydown);
  document.getElementById('moreToolsButton').addEventListener('click', toggleMoreTools);
```

- [ ] **步骤 2：新增键盘处理函数。**

在 `activateSearchResult(tabId)` 后加入：

```js
function handleSearchKeydown(event) {
  const visibleTabs = getVisibleTabs();

  if (event.key === 'ArrowDown') {
    event.preventDefault();
    state.selectedIndex = clampSelectedIndex(state.selectedIndex + 1, visibleTabs.length);
    renderTabs();
    return;
  }

  if (event.key === 'ArrowUp') {
    event.preventDefault();
    state.selectedIndex = clampSelectedIndex(state.selectedIndex - 1, visibleTabs.length);
    renderTabs();
    return;
  }

  if (event.key === 'Enter') {
    event.preventDefault();
    const selectedTab = visibleTabs[clampSelectedIndex(state.selectedIndex, visibleTabs.length)];

    if (!selectedTab) {
      setStatus('没有可切换的标签页');
      return;
    }

    activateSearchResult(selectedTab.id);
  }
}
```

- [ ] **步骤 3：新增更多工具展开函数。**

在 `handleSearchKeydown(event)` 后加入：

```js
function toggleMoreTools() {
  state.moreToolsVisible = !state.moreToolsVisible;
  renderMoreTools();
}

function renderMoreTools() {
  const section = document.getElementById('moreToolsSection');
  const button = document.getElementById('moreToolsButton');

  section.classList.toggle('is-hidden', !state.moreToolsVisible);
  section.hidden = !state.moreToolsVisible;
  button.setAttribute('aria-expanded', state.moreToolsVisible ? 'true' : 'false');
}
```

- [ ] **步骤 4：把更多工具渲染接入总渲染。**

把 `render()` 替换为：

```js
function render() {
  renderSettings();
  renderOverview();
  renderGroups();
  renderDuplicateReview();
  renderTabs();
  renderSessions();
  renderMoreTools();
}
```

- [ ] **步骤 5：让管理操作自动展开更多工具。**

在 `scanDuplicates()` 中 `state.duplicateReview.visible = true;` 后加入：

```js
    state.moreToolsVisible = true;
```

在 `saveWorkspace()` 保存成功后 `state.lastSavedTabIds = result.savedTabIds || [];` 后加入：

```js
    state.moreToolsVisible = true;
```

这些状态更新的原因是用户触发管理动作后需要看到后续确认区或关闭已保存标签入口。

- [ ] **步骤 6：运行语法检查和校验。**

运行：

```bash
node --check popup.js
node scripts/校验插件.js
```

预期结果：两个命令都成功，校验脚本输出 `插件文件校验通过`。

---

## 任务 8：补充搜索优先样式

**文件：**

- 修改：`popup.css`

- [ ] **步骤 1：调整首屏搜索和状态行样式。**

在 `.search-box` 样式后加入：

```css
.quick-search-box {
  margin-top: 14px;
}

.quick-status {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  min-height: 22px;
  margin-top: 10px;
  color: var(--muted);
  font-size: 12px;
}

.quick-status span {
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
```

- [ ] **步骤 2：补充快速结果列表样式。**

在 `.tab-item, .session-item` 样式后加入：

```css
.quick-results-section {
  margin-top: 10px;
}

.quick-result-list {
  max-height: 340px;
}

.quick-result-item {
  width: 100%;
  min-height: 54px;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: center;
  text-align: left;
}

.quick-result-item.is-selected {
  border-color: var(--primary);
  background: #eef5ff;
}

.quick-result-main {
  display: grid;
  min-width: 0;
  gap: 4px;
}

.quick-result-meta {
  max-width: 72px;
  overflow: hidden;
  color: var(--muted);
  font-size: 12px;
  text-overflow: ellipsis;
  white-space: nowrap;
}
```

- [ ] **步骤 3：补充更多工具和管理按钮样式。**

在 `.action-grid` 样式后加入：

```css
.quick-action-grid {
  grid-template-columns: minmax(0, 1fr) auto;
}

.more-tools-section {
  margin-top: 12px;
  padding-top: 12px;
  border-top: 1px solid var(--line);
}

.tool-button-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 8px;
}
```

- [ ] **步骤 4：运行 CSS 相关校验。**

运行：

```bash
node scripts/校验插件.js
```

预期结果：输出 `插件文件校验通过`。

---

## 任务 9：同步 README 和校验文案

**文件：**

- 修改：`README.md`
- 修改：`scripts/校验插件.js`

- [ ] **步骤 1：更新 README 功能特性。**

在 `README.md` 的“功能特性”列表顶部加入：

```markdown
- 打开弹窗即可搜索所有已打开窗口的标签页。
- 未输入搜索词时，默认展示跨窗口最近使用页面。
- 支持点击结果直接切换，也支持上下键选择后回车切换。
```

- [ ] **步骤 2：新增“快速切换”说明。**

在“整理规则”前加入：

```markdown
## 快速切换

弹窗的首要入口是“快速切换”。打开弹窗后，搜索框会自动聚焦，用户可以直接输入标题、网址或主域名来查找已经打开的标签页。

未输入搜索词时，插件会展示所有窗口里的最近使用页面。这样做是为了覆盖“我只是想回到刚才页面”的场景。

搜索结果支持两种切换方式：

- 点击任意结果项，直接切换到对应标签页。
- 使用上下键选择结果，再按回车切换。

搜索只覆盖已打开标签页，不搜索工作集。原因是当前入口面向“切换页面”，而不是恢复历史工作现场。
```

- [ ] **步骤 3：更新开发校验中的 README 断言。**

在 `scripts/校验插件.js` 的 README 断言区域加入：

```js
assert.ok(readmeContent.includes('快速切换'));
assert.ok(readmeContent.includes('跨窗口最近使用页面'));
assert.ok(readmeContent.includes('点击任意结果项'));
```

- [ ] **步骤 4：运行完整校验。**

运行：

```bash
node --check background.js
node --check popup.js
node scripts/校验插件.js
```

预期结果：三个命令都成功，校验脚本输出 `插件文件校验通过`。

---

## 任务 10：最终回归与人工验证

**文件：**

- 校验：`background.js`
- 校验：`popup.js`
- 校验：`popup.html`
- 校验：`popup.css`
- 校验：`README.md`
- 校验：`scripts/校验插件.js`

- [ ] **步骤 1：运行本地完整校验。**

运行：

```bash
node --check background.js
node --check popup.js
node scripts/校验插件.js
```

预期结果：

```text
插件文件校验通过
```

- [ ] **步骤 2：检查工作区变更范围。**

运行：

```bash
git status --short
```

预期结果只包含以下文件：

```text
 M README.md
 M background.js
 M popup.css
 M popup.html
 M popup.js
 M scripts/校验插件.js
?? docs/superpowers/plans/2026-06-05-搜索优先弹窗重构实现计划.md
?? docs/superpowers/specs/2026-06-05-搜索优先弹窗重构设计文档.md
```

如果 `.superpowers/` 出现在结果中，它是头脑风暴视觉辅助目录，不属于实现交付。处理方式是在提交前忽略或清理该目录，原因是它不是插件运行文件。

- [ ] **步骤 3：在 Chrome 开发者模式重载插件后人工验证默认态。**

操作：

1. 打开至少两个 Chrome 窗口。
2. 每个窗口打开两个不同网站标签。
3. 依次切换几个标签，让最近使用时间产生差异。
4. 打开插件弹窗。

预期结果：

- 搜索框自动聚焦。
- 标题显示“快速切换”。
- 未输入时列表展示最近使用页面。
- 结果中能看到“当前窗口”或“其他窗口”标识。

- [ ] **步骤 4：人工验证点击切换。**

操作：

1. 在弹窗中点击一个结果项。
2. 选择一个属于其他窗口的结果项再次点击。

预期结果：

- 点击当前窗口结果时，浏览器切到对应标签。
- 点击其他窗口结果时，目标窗口先置前，再切到对应标签。

- [ ] **步骤 5：人工验证键盘切换。**

操作：

1. 打开弹窗。
2. 输入一个能命中多个标签的关键词。
3. 按上下键移动高亮项。
4. 按回车。

预期结果：

- 上下键只移动高亮，不立即切换。
- 回车切换到当前高亮项。
- 没有结果时按回车显示“没有可切换的标签页”。

- [ ] **步骤 6：人工验证更多工具保留旧能力。**

操作：

1. 点击“更多工具”。
2. 保存工作集。
3. 执行智能去重扫描。
4. 修改分组阈值并保存。
5. 点击优先分组星标。

预期结果：

- 更多工具可以展开和收起。
- 工作集保存、智能去重、阈值保存和优先分组仍然可用。
- 相关状态提示使用中文。

---

## 自检清单

- 搜索首版只覆盖已打开标签页，不搜索工作集。
- 打开弹窗后搜索框自动聚焦。
- 空搜索默认展示跨窗口最近使用页面。
- 点击结果可以直接切换。
- 回车只切换当前高亮结果。
- 上下键不会越界。
- 跨窗口结果会先聚焦窗口再激活标签。
- 现有整理、去重、工作集和设置能力仍有入口。
- 新增注释均为中文，并解释关键边界原因。
- 没有新增英文说明段落。
