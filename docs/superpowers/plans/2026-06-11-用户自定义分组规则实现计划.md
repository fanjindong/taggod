# 用户自定义分组规则 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现用户自定义分组规则，让用户可以用域名、路径、标题等条件把标签归入指定分组，并支持规则级分组阈值。

**Architecture:** 后台新增规则归一化、匹配、最终分组信息计算和规则管理消息；现有整理、优先分组、工作集保存恢复统一改用最终分组键。弹窗在“更多工具”中增加规则管理区域，提供轻量表单，并明确展示留空字段的默认效果。

**Tech Stack:** Chrome Manifest V3、原生 JavaScript、`chrome.tabs`、`chrome.tabGroups`、`chrome.storage.local`、Node.js 校验脚本。

---

## 文件结构

- 修改 `background.js`：新增规则数据归一化、条件匹配、最终分组解析、规则级阈值计算、规则管理消息，并让整理、分组摘要、工作集保存和恢复统一使用最终分组信息。
- 修改 `popup.html`：在“更多工具”里新增“分组规则”区域、规则表单、规则列表和默认值提示文案。
- 修改 `popup.js`：扩展前端状态、渲染规则列表、渲染规则表单、处理新增、编辑、删除、启用、停用、上移、下移和规则阈值默认展示。
- 修改 `popup.css`：补充规则列表、规则表单、条件行、摘要文本和紧凑操作按钮样式。
- 修改 `scripts/校验插件.js`：补充规则归一化、条件匹配、排序聚拢、规则阈值、后台消息和弹窗文案校验。
- 修改 `README.md`：补充用户自定义分组规则说明，保持中文文档。

---

### Task 1: 后台规则模型与纯函数

**Files:**
- Modify: `background.js`
- Test: `scripts/校验插件.js`

- [x] **Step 1: 先写失败校验**

在 `scripts/校验插件.js` 中 `normalizeSettings` 相关断言后追加以下断言：

```js
const normalizedRuleSettings = backgroundSandbox.normalizeSettings({
  minTabsPerGroup: 3,
  groupRules: [
    {
      id: 'rule-old',
      name: '  项目 A  ',
      enabled: true,
      targetGroupKey: 'custom:项目 A',
      targetTitle: '  项目 A  ',
      minTabsPerGroup: 1,
      conditions: [
        { field: 'hostname', operator: 'contains', value: 'github.com' }
      ],
      createdAt: 10,
      updatedAt: 20
    },
    {
      id: '',
      name: '',
      targetTitle: '',
      conditions: []
    }
  ]
});

assert.strictEqual(normalizedRuleSettings.groupRules.length, 1);
assert.strictEqual(normalizedRuleSettings.groupRules[0].name, '项目 A');
assert.strictEqual(normalizedRuleSettings.groupRules[0].targetTitle, '项目 A');
assert.strictEqual(normalizedRuleSettings.groupRules[0].minTabsPerGroup, 1);
assert.strictEqual(normalizedRuleSettings.groupRules[0].conditions[0].value, 'github.com');

const matchedProjectGroup = backgroundSandbox.getResolvedGroupInfo({
  title: '项目 A 问题',
  url: 'https://github.com/my-org/project-a/issues',
  pinned: false
}, normalizedRuleSettings);
assert.strictEqual(matchedProjectGroup.groupKey, 'custom:项目 A');
assert.strictEqual(matchedProjectGroup.title, '项目 A');
assert.strictEqual(matchedProjectGroup.ruleId, 'rule-old');

const fallbackProjectGroup = backgroundSandbox.getResolvedGroupInfo({
  title: '邮箱',
  url: 'https://mail.google.com/inbox',
  pinned: false
}, normalizedRuleSettings);
assert.strictEqual(fallbackProjectGroup.groupKey, 'google.com');
assert.strictEqual(fallbackProjectGroup.title, 'google.com');
assert.strictEqual(fallbackProjectGroup.ruleId, '');

assert.strictEqual(backgroundSandbox.doesRuleMatchTab(normalizedRuleSettings.groupRules[0], {
  title: '项目 A',
  url: 'https://github.com/my-org/project-a',
  pinned: false
}), true);
assert.strictEqual(backgroundSandbox.doesRuleMatchTab(normalizedRuleSettings.groupRules[0], {
  title: '项目 B',
  url: 'https://gitlab.com/my-org/project-a',
  pinned: false
}), false);
```

- [x] **Step 2: 运行校验确认失败**

Run: `node scripts/校验插件.js`

Expected: FAIL，错误包含 `normalizeSettings(...).groupRules` 或 `getResolvedGroupInfo is not a function`。

- [x] **Step 3: 在 `background.js` 增加常量和默认配置**

把 `DEFAULT_SETTINGS` 改成包含 `groupRules`：

```js
const DEFAULT_SETTINGS = {
  // 限制保存数量是为了避免本地存储无限增长，同时保留最近的工作现场。
  maxSessionCount: 10,
  // 默认至少两个同主域名标签才建组，原因是单标签分组通常只会增加标签栏噪音。
  minTabsPerGroup: 2,
  organizeWithGroups: true,
  duplicateKeepStrategy: 'active-or-left',
  priorityGroups: [],
  // 自定义规则默认为空，旧用户升级后仍沿用主域名分组。
  groupRules: []
};
```

在 `TRACKING_QUERY_PARAMS` 后增加字段、操作和条件上限：

```js
const GROUP_RULE_FIELDS = new Set(['hostname', 'primaryDomain', 'path', 'url', 'title']);
const GROUP_RULE_OPERATORS = new Set(['contains', 'equals', 'startsWith']);
const MAX_GROUP_RULE_CONDITIONS = 5;
```

- [x] **Step 4: 在 `background.js` 增加规则归一化函数**

放在 `normalizeMinTabsPerGroup` 后：

```js
function normalizeOptionalMinTabsPerGroup(value) {
  if (value === null || value === undefined || value === '') {
    // 规则阈值为空时沿用全局阈值，避免每条规则都要求重复填写。
    return null;
  }

  const numericValue = typeof value === 'number' ? value : Number.NaN;

  if (!Number.isInteger(numericValue) || numericValue < 1) {
    return null;
  }

  return numericValue;
}

function normalizeGroupRuleCondition(condition) {
  const field = String(condition && condition.field ? condition.field : '').trim();
  const operator = String(condition && condition.operator ? condition.operator : '').trim();
  const value = String(condition && condition.value ? condition.value : '').trim();

  if (!GROUP_RULE_FIELDS.has(field) || !GROUP_RULE_OPERATORS.has(operator) || !value) {
    return null;
  }

  return {
    field,
    operator,
    value
  };
}

function normalizeGroupRule(rule) {
  const now = Date.now();
  const targetTitle = String(rule && rule.targetTitle ? rule.targetTitle : rule && rule.name ? rule.name : '').trim();
  const name = String(rule && rule.name ? rule.name : targetTitle).trim();
  const targetGroupKey = String(rule && rule.targetGroupKey ? rule.targetGroupKey : targetTitle ? `custom:${targetTitle}` : '').trim();
  const conditions = (Array.isArray(rule && rule.conditions) ? rule.conditions : [])
    .map(normalizeGroupRuleCondition)
    .filter(Boolean)
    .slice(0, MAX_GROUP_RULE_CONDITIONS);

  if (!name || !targetTitle || !targetGroupKey || conditions.length === 0) {
    // 无法解释或会误命中的规则直接丢弃，原因是旧配置可能被手动写坏。
    return null;
  }

  return {
    id: String(rule && rule.id ? rule.id : `rule-${now}`),
    name,
    enabled: rule && Object.prototype.hasOwnProperty.call(rule, 'enabled') ? Boolean(rule.enabled) : true,
    targetGroupKey,
    targetTitle,
    minTabsPerGroup: normalizeOptionalMinTabsPerGroup(rule && rule.minTabsPerGroup),
    conditions,
    createdAt: Number(rule && rule.createdAt) || now,
    updatedAt: Number(rule && rule.updatedAt) || now
  };
}

function normalizeGroupRules(groupRules) {
  if (!Array.isArray(groupRules)) {
    return [];
  }

  const usedIds = new Set();

  return groupRules.map(normalizeGroupRule).filter((rule) => {
    if (!rule || usedIds.has(rule.id)) {
      // 重复编号会让编辑和排序目标不明确，保留第一条更符合用户看到的列表顺序。
      return false;
    }

    usedIds.add(rule.id);
    return true;
  });
}
```

- [x] **Step 5: 修改 `normalizeSettings`**

在 `priorityGroups` 归一化后增加：

```js
  normalized.groupRules = normalizeGroupRules(normalized.groupRules);
```

完整函数应保持以下结构：

```js
function normalizeSettings(settings) {
  const normalized = Object.assign({}, DEFAULT_SETTINGS, settings || {});
  normalized.minTabsPerGroup = normalizeMinTabsPerGroup(normalized.minTabsPerGroup);

  if (!Array.isArray(normalized.priorityGroups)) {
    // 旧版本没有该字段，兜底为空数组可以兼容已经安装过的用户配置。
    normalized.priorityGroups = [];
  }

  normalized.groupRules = normalizeGroupRules(normalized.groupRules);

  return normalized;
}
```

- [x] **Step 6: 增加匹配和最终分组函数**

放在 `getHostnameKey` 后：

```js
function buildRuleMatchContext(tab) {
  const url = tab && tab.url ? tab.url : '';

  try {
    const parsedUrl = new URL(url);
    const hostname = String(parsedUrl.hostname || '').toLowerCase().replace(/\.$/, '');

    return {
      hostname,
      primaryDomain: getPrimaryDomainFromHostname(hostname),
      path: parsedUrl.pathname || '/',
      url,
      title: tab && tab.title ? tab.title : ''
    };
  } catch (error) {
    // 异常网址仍允许用标题匹配，其他 URL 字段用空值避免误判。
    return {
      hostname: '其他',
      primaryDomain: '其他',
      path: '',
      url,
      title: tab && tab.title ? tab.title : ''
    };
  }
}

function doesRuleConditionMatch(context, condition) {
  const actualValue = String(context[condition.field] || '').toLowerCase();
  const expectedValue = String(condition.value || '').toLowerCase();

  if (condition.operator === 'equals') {
    return actualValue === expectedValue;
  }

  if (condition.operator === 'startsWith') {
    return actualValue.startsWith(expectedValue);
  }

  return actualValue.includes(expectedValue);
}

function doesRuleMatchTab(rule, tab) {
  if (!rule || !rule.enabled || !Array.isArray(rule.conditions) || rule.conditions.length === 0) {
    return false;
  }

  const context = buildRuleMatchContext(tab);

  return rule.conditions.every((condition) => doesRuleConditionMatch(context, condition));
}

function getResolvedGroupInfo(tab, settings) {
  const normalizedSettings = normalizeSettings(settings);

  if (tab && tab.pinned) {
    return {
      groupKey: getDomainKey(tab.url || ''),
      title: getDomainKey(tab.url || ''),
      ruleId: '',
      minTabsPerGroup: normalizedSettings.minTabsPerGroup
    };
  }

  for (const rule of normalizedSettings.groupRules) {
    if (doesRuleMatchTab(rule, tab)) {
      return {
        groupKey: rule.targetGroupKey,
        title: rule.targetTitle,
        ruleId: rule.id,
        minTabsPerGroup: rule.minTabsPerGroup
      };
    }
  }

  const groupKey = getDomainKey(tab && tab.url ? tab.url : '');

  return {
    groupKey,
    title: groupKey,
    ruleId: '',
    minTabsPerGroup: normalizedSettings.minTabsPerGroup
  };
}
```

- [x] **Step 7: 运行校验确认通过新增纯函数测试**

Run: `node scripts/校验插件.js`

Expected: PASS，输出 `插件校验通过` 或脚本正常结束。

---

### Task 2: 最终分组键接入整理、摘要和工作集

**Files:**
- Modify: `background.js`
- Test: `scripts/校验插件.js`

- [x] **Step 1: 写排序聚拢和规则阈值失败校验**

在 `scripts/校验插件.js` 中 `organizedTabsWithSoloFirst` 断言后追加：

```js
const customRuleSettings = backgroundSandbox.normalizeSettings({
  minTabsPerGroup: 3,
  priorityGroups: [],
  groupRules: [
    {
      id: 'rule-project-a-github',
      name: '项目 A 仓库',
      enabled: true,
      targetGroupKey: 'custom:项目 A',
      targetTitle: '项目 A',
      minTabsPerGroup: null,
      conditions: [{ field: 'hostname', operator: 'contains', value: 'github.com' }],
      createdAt: 1,
      updatedAt: 1
    },
    {
      id: 'rule-project-a-doc',
      name: '项目 A 文档',
      enabled: true,
      targetGroupKey: 'custom:项目 A',
      targetTitle: '项目 A',
      minTabsPerGroup: null,
      conditions: [{ field: 'hostname', operator: 'contains', value: 'docs.example.com' }],
      createdAt: 2,
      updatedAt: 2
    }
  ]
});

const customOrganizedTabs = backgroundSandbox.buildOrganizedTabs([
  { id: 401, title: '零散', url: 'https://solo.example.com', pinned: false, index: 0 },
  { id: 402, title: '仓库', url: 'https://github.com/my-org/project-a', pinned: false, index: 1 },
  { id: 403, title: '其他', url: 'https://other.example.com', pinned: false, index: 2 },
  { id: 404, title: '文档', url: 'https://docs.example.com/project-a', pinned: false, index: 3 }
], customRuleSettings);
assert.deepStrictEqual(Array.from(customOrganizedTabs, (tab) => tab.id), [402, 404, 403, 401]);

const customGroupSummaries = backgroundSandbox.buildGroupSummaries([
  { id: 411, title: '仓库', url: 'https://github.com/my-org/project-a', pinned: false, index: 0 },
  { id: 412, title: '文档', url: 'https://docs.example.com/project-a', pinned: false, index: 1 }
], customRuleSettings);
assert.strictEqual(customGroupSummaries.length, 0);

const customRuleThresholdSummaries = backgroundSandbox.buildGroupSummaries([
  { id: 421, title: '仓库', url: 'https://github.com/my-org/project-a', pinned: false, index: 0 }
], backgroundSandbox.normalizeSettings({
  minTabsPerGroup: 3,
  priorityGroups: [],
  groupRules: [
    {
      id: 'rule-project-a-one',
      name: '项目 A',
      enabled: true,
      targetGroupKey: 'custom:项目 A',
      targetTitle: '项目 A',
      minTabsPerGroup: 1,
      conditions: [{ field: 'hostname', operator: 'contains', value: 'github.com' }],
      createdAt: 1,
      updatedAt: 1
    }
  ]
}));
assert.strictEqual(customRuleThresholdSummaries.length, 1);
assert.strictEqual(customRuleThresholdSummaries[0].groupKey, 'custom:项目 A');
assert.strictEqual(customRuleThresholdSummaries[0].title, '项目 A');
```

- [x] **Step 2: 运行校验确认失败**

Run: `node scripts/校验插件.js`

Expected: FAIL，排序仍按旧主域名或摘要没有识别自定义规则。

- [x] **Step 3: 让标签快照使用最终分组信息**

修改 `buildTabSnapshot` 和 `buildTabSnapshots`：

```js
function buildResolvedGroupTitleMap(tabs, settings = DEFAULT_SETTINGS) {
  const normalizedSettings = normalizeSettings(settings);
  const groupInfos = (Array.isArray(tabs) ? tabs : []).map((tab) => getResolvedGroupInfo(tab, normalizedSettings));
  const domainTitleMap = buildGroupTitleMap(groupInfos
    .filter((info) => !info.ruleId)
    .map((info) => info.groupKey));
  const titleMap = new Map(domainTitleMap);

  groupInfos.forEach((info) => {
    if (info.ruleId && !titleMap.has(info.groupKey)) {
      // 自定义分组标题来自用户输入，不经过公共后缀裁剪，避免 custom:项目 A 被直接显示出来。
      titleMap.set(info.groupKey, info.title);
    }
  });

  return titleMap;
}

function buildTabSnapshot(tab, settings = DEFAULT_SETTINGS) {
  const groupInfo = getResolvedGroupInfo(tab, settings);

  return {
    id: tab.id,
    title: tab.title || '未命名标签',
    url: tab.url || '',
    favIconUrl: tab.favIconUrl || '',
    active: Boolean(tab.active),
    pinned: Boolean(tab.pinned),
    index: Number.isInteger(tab.index) ? tab.index : 0,
    groupKey: groupInfo.groupKey,
    groupTitle: groupInfo.title
  };
}

function buildTabSnapshots(tabs, settings = DEFAULT_SETTINGS) {
  const snapshots = tabs.map((tab) => buildTabSnapshot(tab, settings));
  const titleMap = buildResolvedGroupTitleMap(tabs, settings);

  return snapshots.map((snapshot) => Object.assign({}, snapshot, {
    groupTitle: titleMap.get(snapshot.groupKey) || snapshot.groupTitle || snapshot.groupKey
  }));
}
```

- [x] **Step 4: 新增最终分组阈值辅助函数**

放在 `shouldCreateNativeGroup` 后：

```js
function resolveGroupThreshold(groupTabs, settings) {
  const normalizedSettings = normalizeSettings(settings);
  const safeGroupTabs = Array.isArray(groupTabs) ? groupTabs : [];
  const firstGroupInfo = safeGroupTabs.length > 0 ? getResolvedGroupInfo(safeGroupTabs[0], normalizedSettings) : null;

  if (firstGroupInfo && firstGroupInfo.ruleId) {
    for (const rule of normalizedSettings.groupRules) {
      if (rule.targetGroupKey !== firstGroupInfo.groupKey) {
        continue;
      }

      if (!Number.isInteger(rule.minTabsPerGroup) || rule.minTabsPerGroup < 1) {
        continue;
      }

      if (safeGroupTabs.some((tab) => doesRuleMatchTab(rule, tab))) {
        // 同一自定义分组可能由多条规则合并，阈值按规则列表顺序选择第一条有效配置。
        return rule.minTabsPerGroup;
      }
    }
  }

  return normalizedSettings.minTabsPerGroup;
}

function shouldCreateNativeGroupForTabs(groupTabs, settings) {
  const safeGroupTabs = Array.isArray(groupTabs) ? groupTabs : [];
  const threshold = resolveGroupThreshold(safeGroupTabs, settings);

  return safeGroupTabs.length >= threshold;
}
```

- [x] **Step 5: 修改当前顺序和可建组集合**

把 `buildCurrentGroupOrderMap` 改为：

```js
function buildCurrentGroupOrderMap(tabs, settings = DEFAULT_SETTINGS) {
  const orderMap = new Map();
  const normalizedSettings = normalizeSettings(settings);

  tabs.forEach((tab) => {
    if (tab.pinned) {
      return;
    }

    const groupKey = getResolvedGroupInfo(tab, normalizedSettings).groupKey;

    if (!orderMap.has(groupKey)) {
      // 当前从左到右的首次出现顺序代表用户刚刚手动拖好的分组顺序。
      orderMap.set(groupKey, orderMap.size);
    }
  });

  return orderMap;
}
```

把 `buildGroupableDomainSet` 替换为：

```js
function buildGroupableDomainSet(tabs, settings) {
  const normalizedSettings = normalizeSettings(settings);
  const groupTabsMap = new Map();

  (Array.isArray(tabs) ? tabs : []).forEach((tab) => {
    if (tab.pinned || typeof tab.id !== 'number') {
      return;
    }

    const groupKey = getResolvedGroupInfo(tab, normalizedSettings).groupKey;
    const groupTabs = groupTabsMap.get(groupKey) || [];
    groupTabs.push(tab);
    groupTabsMap.set(groupKey, groupTabs);
  });

  return new Set(Array.from(groupTabsMap.entries())
    .filter(([, groupTabs]) => shouldCreateNativeGroupForTabs(groupTabs, normalizedSettings))
    .map(([groupKey]) => groupKey));
}
```

- [x] **Step 6: 修改 `buildOrganizedTabs` 排序**

在 `buildOrganizedTabs` 内使用最终分组键：

```js
function buildOrganizedTabs(tabs, settings) {
  const safeTabs = Array.isArray(tabs) ? tabs : [];
  const normalizedSettings = normalizeSettings(settings);
  const currentGroupOrderMap = buildCurrentGroupOrderMap(safeTabs, normalizedSettings);
  const groupableDomainSet = buildGroupableDomainSet(safeTabs, normalizedSettings);

  return [...safeTabs].sort((left, right) => {
    if (left.pinned !== right.pinned) {
      return left.pinned ? -1 : 1;
    }

    const leftInfo = getResolvedGroupInfo(left, normalizedSettings);
    const rightInfo = getResolvedGroupInfo(right, normalizedSettings);
    const leftDomain = leftInfo.groupKey;
    const rightDomain = rightInfo.groupKey;
    const leftIsGroupable = groupableDomainSet.has(leftDomain);
    const rightIsGroupable = groupableDomainSet.has(rightDomain);

    if (leftIsGroupable !== rightIsGroupable) {
      // 阈值只决定是否创建原生分组；未达阈值的同项目标签仍会在后续按最终分组键聚拢。
      return leftIsGroupable ? -1 : 1;
    }

    const leftIsPriority = isPriorityGroup(normalizedSettings, leftDomain);
    const rightIsPriority = isPriorityGroup(normalizedSettings, rightDomain);

    if (leftIsPriority !== rightIsPriority) {
      return leftIsPriority ? -1 : 1;
    }

    const leftCurrentRank = currentGroupOrderMap.get(leftDomain) ?? Number.POSITIVE_INFINITY;
    const rightCurrentRank = currentGroupOrderMap.get(rightDomain) ?? Number.POSITIVE_INFINITY;

    if (leftCurrentRank !== rightCurrentRank) {
      return leftCurrentRank - rightCurrentRank;
    }

    const domainCompare = leftDomain.localeCompare(rightDomain, 'zh-CN');

    if (domainCompare !== 0) {
      return domainCompare;
    }

    const leftHostname = getHostnameKey(left.url || '');
    const rightHostname = getHostnameKey(right.url || '');
    const hostnameCompare = leftHostname.localeCompare(rightHostname, 'zh-CN');

    return hostnameCompare === 0 ? left.index - right.index : hostnameCompare;
  });
}
```

- [x] **Step 7: 修改 `reconcileCurrentWindowGroups` 和 `buildGroupSummaries`**

在两个函数里把 `getDomainKey(tab.url || '')` 替换为：

```js
const groupInfo = getResolvedGroupInfo(tab, normalizedSettings);
const groupKey = groupInfo.groupKey;
```

`reconcileCurrentWindowGroups` 中判断阈值改为：

```js
    if (!shouldCreateNativeGroupForTabs(groupTabs, normalizedSettings)) {
```

`reconcileCurrentWindowGroups` 中标题映射改为：

```js
  const titleMap = buildResolvedGroupTitleMap(tabs, normalizedSettings);
```

`buildGroupSummaries` 的 `summary` 新增 `title: groupInfo.title`，过滤阈值改为：

```js
  const groupSummaries = Array.from(groupMap.values()).filter((summary) => {
    // 优先分组列表只展示真实会创建原生分组的项，避免数量不足的分组出现无意义星标入口。
    return shouldCreateNativeGroupForTabs(summary.tabs, normalizedSettings);
  });
```

构建 `summary` 时保存原始标签数组：

```js
      tabs: [],
```

每次命中后追加：

```js
    summary.tabs.push(tab);
```

`buildGroupSummaries` 中标题映射改为：

```js
  const titleMap = buildResolvedGroupTitleMap(
    groupSummaries.flatMap((summary) => summary.tabs),
    normalizedSettings
  );
```

- [x] **Step 8: 修改保存和恢复工作集**

在 `saveWorkspace` 中改为：

```js
  const snapshots = buildTabSnapshots(tabs, settings);
```

在 `restoreSession` 创建标签后不要沿用旧 `groupKey`，让当前规则重新计算：

```js
      createdTabs.push(Object.assign({}, createdWindow.tabs[0], {
        title: firstRestorableTab.title || createdWindow.tabs[0].title || '',
        url: firstRestorableTab.url
      }));
```

和：

```js
      createdTabs.push(Object.assign({}, createdTab, {
        title: tab.title || createdTab.title || '',
        url: tab.url
      }));
```

在 `regroupRestoredTabs` 中把：

```js
    const groupKey = tab.groupKey || getDomainKey(tab.url || '');
```

替换为：

```js
    const groupKey = getResolvedGroupInfo(tab, normalizedSettings).groupKey;
```

并在函数开头增加：

```js
  const normalizedSettings = normalizeSettings(settings);
```

`regroupRestoredTabs` 中标题映射改为：

```js
  const titleMap = buildResolvedGroupTitleMap(tabs, normalizedSettings);
```

阈值判断改为：

```js
    const groupTabs = tabs.filter((tab) => getResolvedGroupInfo(tab, normalizedSettings).groupKey === groupKey);

    if (!shouldCreateNativeGroupForTabs(groupTabs, normalizedSettings)) {
```

把 `buildGroupSnapshots` 末尾的短标题覆盖逻辑移除，保留快照里的 `groupTitle`：

```js
function buildGroupSnapshots(tabs) {
  const groupMap = new Map();

  tabs.forEach((tab) => {
    const groupKey = tab.groupKey || '其他';
    const snapshot = groupMap.get(groupKey) || {
      groupKey,
      title: tab.groupTitle || groupKey,
      color: getGroupColor(groupMap.size),
      tabCount: 0
    };

    snapshot.tabCount += 1;
    groupMap.set(groupKey, snapshot);
  });

  return Array.from(groupMap.values());
}
```

- [x] **Step 9: 运行校验**

Run:

```bash
node --check background.js
node scripts/校验插件.js
```

Expected: 两条命令都成功。

---

### Task 3: 后台规则管理消息

**Files:**
- Modify: `background.js`
- Test: `scripts/校验插件.js`

- [x] **Step 1: 写后台消息失败校验**

在 `scripts/校验插件.js` 的 `runAsyncChecks()` 末尾、`console.log` 之前追加：

```js
  const storedState = {
    'tabgod.settings': backgroundSandbox.normalizeSettings({
      minTabsPerGroup: 2,
      priorityGroups: [],
      groupRules: []
    })
  };

  backgroundSandbox.chrome.storage = {
    local: {
      get: async (keys) => {
        const result = {};
        keys.forEach((key) => {
          result[key] = storedState[key];
        });
        return result;
      },
      set: async (values) => {
        Object.assign(storedState, values);
      }
    }
  };

  const createdRuleResult = await backgroundSandbox.handleMessage({
    action: 'create-group-rule',
    rule: {
      name: '',
      targetTitle: '项目 A',
      minTabsPerGroup: '',
      conditions: [{ field: 'hostname', operator: 'contains', value: 'github.com' }]
    }
  });
  assert.strictEqual(createdRuleResult.rule.name, '项目 A');
  assert.strictEqual(createdRuleResult.rule.minTabsPerGroup, null);
  assert.strictEqual(createdRuleResult.settings.groupRules.length, 1);

  const updatedRuleResult = await backgroundSandbox.handleMessage({
    action: 'update-group-rule',
    ruleId: createdRuleResult.rule.id,
    rule: {
      enabled: false,
      minTabsPerGroup: 1
    }
  });
  assert.strictEqual(updatedRuleResult.rule.enabled, false);
  assert.strictEqual(updatedRuleResult.rule.minTabsPerGroup, 1);

  const movedRuleResult = await backgroundSandbox.handleMessage({
    action: 'move-group-rule',
    ruleId: createdRuleResult.rule.id,
    direction: 'up'
  });
  assert.strictEqual(movedRuleResult.settings.groupRules.length, 1);

  const deletedRuleResult = await backgroundSandbox.handleMessage({
    action: 'delete-group-rule',
    ruleId: createdRuleResult.rule.id
  });
  assert.strictEqual(deletedRuleResult.settings.groupRules.length, 0);
```

- [x] **Step 2: 运行校验确认失败**

Run: `node scripts/校验插件.js`

Expected: FAIL，错误包含 `未知操作`。

- [x] **Step 3: 增加规则创建校验函数**

放在 `togglePriorityGroup` 后：

```js
function buildRuleForCreate(rule) {
  const now = Date.now();
  const targetTitle = String(rule && rule.targetTitle ? rule.targetTitle : rule && rule.name ? rule.name : '').trim();
  const name = String(rule && rule.name ? rule.name : targetTitle).trim();

  return normalizeGroupRule(Object.assign({}, rule, {
    id: `rule-${now}`,
    name,
    targetTitle,
    targetGroupKey: rule && rule.targetGroupKey ? rule.targetGroupKey : `custom:${targetTitle}`,
    createdAt: now,
    updatedAt: now
  }));
}

function assertValidGroupRule(rule) {
  if (!rule) {
    throw new Error('分组规则不能为空');
  }

  if (!rule.name) {
    throw new Error('规则名称不能为空');
  }

  if (!rule.targetTitle) {
    throw new Error('目标分组名不能为空');
  }

  if (!Array.isArray(rule.conditions) || rule.conditions.length === 0) {
    throw new Error('至少需要一个匹配条件');
  }

  if (rule.conditions.length > MAX_GROUP_RULE_CONDITIONS) {
    throw new Error(`每条规则最多 ${MAX_GROUP_RULE_CONDITIONS} 个条件`);
  }
}
```

- [x] **Step 4: 增加规则存储更新函数**

放在 `updateSettings` 前：

```js
async function updateStoredSettings(updater) {
  const stored = await chrome.storage.local.get([STORAGE_KEYS.settings]);
  const settings = normalizeSettings(stored[STORAGE_KEYS.settings]);
  const nextSettings = normalizeSettings(updater(settings));

  await chrome.storage.local.set({ [STORAGE_KEYS.settings]: nextSettings });

  return nextSettings;
}
```

- [x] **Step 5: 增加创建、更新、删除、移动规则函数**

放在 `updateStoredSettings` 后：

```js
async function createGroupRule(ruleInput) {
  let createdRule = null;
  const settings = await updateStoredSettings((settings) => {
    createdRule = buildRuleForCreate(ruleInput);
    assertValidGroupRule(createdRule);

    return Object.assign({}, settings, {
      groupRules: [...settings.groupRules, createdRule]
    });
  });

  return {
    rule: createdRule,
    settings
  };
}

async function updateGroupRule(ruleId, partialRule) {
  let updatedRule = null;
  const settings = await updateStoredSettings((settings) => {
    const groupRules = settings.groupRules.map((rule) => {
      if (rule.id !== ruleId) {
        return rule;
      }

      const nextRule = normalizeGroupRule(Object.assign({}, rule, partialRule || {}, {
        id: rule.id,
        targetGroupKey: rule.targetGroupKey,
        updatedAt: Date.now()
      }));
      assertValidGroupRule(nextRule);
      updatedRule = nextRule;
      return nextRule;
    });

    return Object.assign({}, settings, { groupRules });
  });

  if (!updatedRule) {
    throw new Error('没有找到要更新的分组规则');
  }

  return {
    rule: updatedRule,
    settings
  };
}

async function deleteGroupRule(ruleId) {
  let deletedCount = 0;
  const settings = await updateStoredSettings((settings) => {
    return Object.assign({}, settings, {
      groupRules: settings.groupRules.filter((rule) => {
        if (rule.id === ruleId) {
          deletedCount += 1;
          return false;
        }

        return true;
      })
    });
  });

  if (deletedCount === 0) {
    throw new Error('没有找到要删除的分组规则');
  }

  return {
    deletedCount,
    settings
  };
}

async function moveGroupRule(ruleId, direction) {
  let moved = false;
  const settings = await updateStoredSettings((settings) => {
    const groupRules = [...settings.groupRules];
    const currentIndex = groupRules.findIndex((rule) => rule.id === ruleId);
    const offset = direction === 'down' ? 1 : -1;
    const nextIndex = currentIndex + offset;

    if (currentIndex < 0) {
      throw new Error('没有找到要移动的分组规则');
    }

    if (nextIndex < 0 || nextIndex >= groupRules.length) {
      return settings;
    }

    const [rule] = groupRules.splice(currentIndex, 1);
    groupRules.splice(nextIndex, 0, rule);
    moved = true;

    return Object.assign({}, settings, { groupRules });
  });

  return {
    moved,
    settings
  };
}
```

- [x] **Step 6: 修改 `updateSettings` 复用新工具**

把 `updateSettings` 改为：

```js
async function updateSettings(partialSettings) {
  const settings = await updateStoredSettings((settings) => {
    return Object.assign({}, settings, partialSettings || {});
  });
  const reconcileResult = await reconcileCurrentWindowGroups(settings);

  return Object.assign({
    settings
  }, reconcileResult);
}
```

- [x] **Step 7: 接入 `handleMessage`**

在 `update-settings` 分支前增加：

```js
  if (action === 'create-group-rule') {
    return createGroupRule(message.rule);
  }

  if (action === 'update-group-rule') {
    return updateGroupRule(message.ruleId, message.rule);
  }

  if (action === 'delete-group-rule') {
    return deleteGroupRule(message.ruleId);
  }

  if (action === 'move-group-rule') {
    return moveGroupRule(message.ruleId, message.direction);
  }
```

- [x] **Step 8: 运行校验**

Run:

```bash
node --check background.js
node scripts/校验插件.js
```

Expected: 两条命令都成功。

---

### Task 4: 弹窗规则管理结构

**Files:**
- Modify: `popup.html`
- Test: `scripts/校验插件.js`

- [x] **Step 1: 写弹窗结构失败校验**

在 `scripts/校验插件.js` 的 `popupHtml` 断言附近追加：

```js
assert.ok(popupHtml.includes('groupRuleList'));
assert.ok(popupHtml.includes('groupRuleForm'));
assert.ok(popupHtml.includes('新增分组规则'));
assert.ok(popupHtml.includes('留空使用全局阈值'));
assert.ok(popupHtml.includes('当前全局'));
assert.ok(popupHtml.includes('保存后参与整理规则'));
```

- [x] **Step 2: 运行校验确认失败**

Run: `node scripts/校验插件.js`

Expected: FAIL，错误来自新增 HTML 文案断言。

- [x] **Step 3: 修改 `popup.html` 增加规则区域**

在“管理操作”区域后、“优先分组”区域前插入：

```html
        <section class="list-section group-rule-section" aria-label="分组规则">
          <div class="section-title">
            <h2>分组规则</h2>
            <button id="newGroupRuleButton" class="compact-button" type="button">新增分组规则</button>
          </div>
          <p class="helper-text section-helper-text">规则从上到下匹配，第一条命中决定标签归到哪个分组。</p>

          <form id="groupRuleForm" class="group-rule-form is-hidden" hidden>
            <label class="rule-field">
              <span>规则名称</span>
              <input id="groupRuleNameInput" type="text" placeholder="默认使用目标分组名">
              <small id="groupRuleNamePreview">未填写时会使用目标分组名。</small>
            </label>

            <label class="rule-field">
              <span>目标分组名</span>
              <input id="groupRuleTargetTitleInput" type="text" placeholder="例如：项目 A">
              <small>用于创建分组标题，也会生成稳定的自定义分组标识。</small>
            </label>

            <label class="rule-field">
              <span>分组阈值</span>
              <input id="groupRuleThresholdInput" type="number" min="1" step="1" inputmode="numeric" placeholder="留空使用全局阈值">
              <small id="groupRuleThresholdHelp">当前全局：至少 2 个标签。</small>
            </label>

            <label class="rule-toggle">
              <input id="groupRuleEnabledInput" type="checkbox" checked>
              <span>保存后参与整理规则</span>
            </label>

            <div class="condition-editor" aria-label="匹配条件">
              <div class="condition-editor-header">
                <span>匹配条件</span>
                <button id="addRuleConditionButton" class="compact-button" type="button">添加条件</button>
              </div>
              <div id="groupRuleConditionList" class="condition-list"></div>
              <p class="helper-text">条件值没有默认值，必须填写后才会参与匹配。</p>
            </div>

            <div class="inline-actions">
              <button id="saveGroupRuleButton" class="primary-action" type="submit">保存规则</button>
              <button id="cancelGroupRuleButton" type="button">取消</button>
            </div>
          </form>

          <div id="groupRuleList" class="group-rule-list"></div>
        </section>
```

- [x] **Step 4: 运行校验**

Run:

```bash
node --check popup.js
node scripts/校验插件.js
```

Expected: 两条命令都成功。

---

### Task 5: 弹窗规则状态与交互

**Files:**
- Modify: `popup.js`
- Test: `scripts/校验插件.js`

- [x] **Step 1: 写前端函数和文案失败校验**

在 `scripts/校验插件.js` 的 `popupSandbox` 断言后追加：

```js
assert.strictEqual(typeof popupSandbox.formatGroupRuleThresholdText, 'function');
assert.strictEqual(popupSandbox.formatGroupRuleThresholdText(null, 2), '使用全局阈值：至少 2 个标签');
assert.strictEqual(popupSandbox.formatGroupRuleThresholdText(1, 2), '规则阈值：至少 1 个标签');
assert.strictEqual(typeof popupSandbox.createDefaultGroupRuleDraft, 'function');
assert.strictEqual(popupSandbox.createDefaultGroupRuleDraft().enabled, true);
assert.strictEqual(popupSandbox.createDefaultGroupRuleDraft().conditions[0].field, 'hostname');
```

- [x] **Step 2: 运行校验确认失败**

Run: `node scripts/校验插件.js`

Expected: FAIL，错误包含 `formatGroupRuleThresholdText`。

- [x] **Step 3: 扩展 `state`**

在 `state.settings` 内加入 `groupRules: []`，并在 `state` 末尾加入规则表单状态：

```js
  ruleEditor: {
    visible: false,
    editingRuleId: '',
    draft: null
  }
```

- [x] **Step 4: 绑定规则按钮事件**

在 `bindEvents()` 末尾增加：

```js
  document.getElementById('newGroupRuleButton').addEventListener('click', openNewGroupRuleForm);
  document.getElementById('groupRuleForm').addEventListener('submit', saveGroupRule);
  document.getElementById('cancelGroupRuleButton').addEventListener('click', closeGroupRuleForm);
  document.getElementById('addRuleConditionButton').addEventListener('click', addRuleCondition);
  document.getElementById('groupRuleNameInput').addEventListener('input', updateRuleDraftFromForm);
  document.getElementById('groupRuleTargetTitleInput').addEventListener('input', updateRuleDraftFromForm);
  document.getElementById('groupRuleThresholdInput').addEventListener('input', updateRuleDraftFromForm);
  document.getElementById('groupRuleEnabledInput').addEventListener('change', updateRuleDraftFromForm);
```

- [x] **Step 5: 增加规则草稿和展示辅助函数**

放在 `renderSettings()` 后：

```js
function createDefaultGroupRuleDraft() {
  return {
    name: '',
    enabled: true,
    targetTitle: '',
    minTabsPerGroup: null,
    conditions: [
      {
        field: 'hostname',
        operator: 'contains',
        value: ''
      }
    ]
  };
}

function formatGroupRuleThresholdText(minTabsPerGroup, globalMinTabsPerGroup) {
  const globalThreshold = Number(globalMinTabsPerGroup) || 2;

  if (!Number.isInteger(minTabsPerGroup) || minTabsPerGroup < 1) {
    return `使用全局阈值：至少 ${globalThreshold} 个标签`;
  }

  return `规则阈值：至少 ${minTabsPerGroup} 个标签`;
}

function formatGroupRuleCondition(condition) {
  const fieldLabelMap = {
    hostname: '域名',
    primaryDomain: '主域名',
    path: '路径',
    url: '网址',
    title: '标题'
  };
  const operatorLabelMap = {
    contains: '包含',
    equals: '等于',
    startsWith: '开头是'
  };

  return `${fieldLabelMap[condition.field] || condition.field}${operatorLabelMap[condition.operator] || condition.operator} ${condition.value || '未填写'}`;
}

function formatGroupRuleSummary(rule) {
  const conditionText = (rule.conditions || []).map(formatGroupRuleCondition).join('，');
  const thresholdText = formatGroupRuleThresholdText(rule.minTabsPerGroup, state.settings.minTabsPerGroup);
  const enabledText = rule.enabled ? '已启用' : '已停用，不参与匹配';

  return `${conditionText} · ${thresholdText} · ${enabledText}`;
}
```

- [x] **Step 6: 在 `render()` 中接入规则渲染**

修改 `render()`：

```js
function render() {
  renderSettings();
  renderOverview();
  renderGroupRules();
  renderGroupRuleForm();
  renderGroups();
  renderDuplicateReview();
  renderSessions();
  renderMoreTools();
}
```

- [x] **Step 7: 实现规则列表渲染**

在 `renderGroups()` 前增加：

```js
function renderGroupRules() {
  const list = document.getElementById('groupRuleList');
  const rules = state.settings.groupRules || [];
  list.innerHTML = '';

  if (rules.length === 0) {
    list.appendChild(createEmptyState('还没有自定义分组规则'));
    return;
  }

  rules.forEach((rule, index) => {
    const item = document.createElement('article');
    item.className = `group-rule-item${rule.enabled ? '' : ' is-disabled'}`;
    item.innerHTML = `
      <div class="group-rule-main">
        <strong title="${escapeHtml(rule.name)}">${escapeHtml(rule.name)}</strong>
        <span title="${escapeHtml(rule.targetTitle)}">归入：${escapeHtml(rule.targetTitle)}</span>
        <span title="${escapeHtml(formatGroupRuleSummary(rule))}">${escapeHtml(formatGroupRuleSummary(rule))}</span>
      </div>
      <div class="inline-actions group-rule-actions">
        <button type="button" data-action="toggle-rule" data-rule-id="${escapeHtml(rule.id)}">${rule.enabled ? '停用' : '启用'}</button>
        <button type="button" data-action="move-rule-up" data-rule-id="${escapeHtml(rule.id)}" ${index === 0 ? 'disabled' : ''}>上移</button>
        <button type="button" data-action="move-rule-down" data-rule-id="${escapeHtml(rule.id)}" ${index === rules.length - 1 ? 'disabled' : ''}>下移</button>
        <button type="button" data-action="edit-rule" data-rule-id="${escapeHtml(rule.id)}">编辑</button>
        <button class="danger-button" type="button" data-action="delete-rule" data-rule-id="${escapeHtml(rule.id)}">删除</button>
      </div>
    `;
    list.appendChild(item);
  });

  list.querySelectorAll('button[data-rule-id]').forEach((button) => {
    button.addEventListener('click', () => handleGroupRuleAction(button.dataset.action, button.dataset.ruleId));
  });
}
```

- [x] **Step 8: 实现规则表单渲染**

在 `renderGroupRules()` 后增加：

```js
function renderGroupRuleForm() {
  const form = document.getElementById('groupRuleForm');
  const draft = state.ruleEditor.draft || createDefaultGroupRuleDraft();
  const conditionList = document.getElementById('groupRuleConditionList');

  form.classList.toggle('is-hidden', !state.ruleEditor.visible);
  form.hidden = !state.ruleEditor.visible;

  if (!state.ruleEditor.visible) {
    return;
  }

  document.getElementById('groupRuleNameInput').value = draft.name || '';
  document.getElementById('groupRuleTargetTitleInput').value = draft.targetTitle || '';
  document.getElementById('groupRuleThresholdInput').value = Number.isInteger(draft.minTabsPerGroup) ? String(draft.minTabsPerGroup) : '';
  document.getElementById('groupRuleEnabledInput').checked = draft.enabled !== false;
  document.getElementById('groupRuleThresholdHelp').textContent = `当前全局：至少 ${state.settings.minTabsPerGroup || 2} 个标签。`;

  const previewName = draft.name || draft.targetTitle || '';
  document.getElementById('groupRuleNamePreview').textContent = previewName ? `保存后规则名称：${previewName}` : '未填写时会使用目标分组名。';

  conditionList.innerHTML = '';
  draft.conditions.forEach((condition, index) => {
    const row = document.createElement('div');
    row.className = 'condition-row';
    row.innerHTML = `
      <select data-condition-index="${index}" data-condition-field="field">
        <option value="hostname" ${condition.field === 'hostname' ? 'selected' : ''}>域名</option>
        <option value="primaryDomain" ${condition.field === 'primaryDomain' ? 'selected' : ''}>主域名</option>
        <option value="path" ${condition.field === 'path' ? 'selected' : ''}>路径</option>
        <option value="url" ${condition.field === 'url' ? 'selected' : ''}>网址</option>
        <option value="title" ${condition.field === 'title' ? 'selected' : ''}>标题</option>
      </select>
      <select data-condition-index="${index}" data-condition-field="operator">
        <option value="contains" ${condition.operator === 'contains' ? 'selected' : ''}>包含</option>
        <option value="equals" ${condition.operator === 'equals' ? 'selected' : ''}>等于</option>
        <option value="startsWith" ${condition.operator === 'startsWith' ? 'selected' : ''}>开头是</option>
      </select>
      <input type="text" data-condition-index="${index}" data-condition-field="value" value="${escapeHtml(condition.value || '')}" placeholder="必须填写匹配内容">
      <button type="button" data-remove-condition-index="${index}" ${draft.conditions.length <= 1 ? 'disabled' : ''}>删除</button>
    `;
    conditionList.appendChild(row);
  });

  conditionList.querySelectorAll('[data-condition-index]').forEach((input) => {
    input.addEventListener('input', updateRuleDraftFromForm);
    input.addEventListener('change', updateRuleDraftFromForm);
  });
  conditionList.querySelectorAll('button[data-remove-condition-index]').forEach((button) => {
    button.addEventListener('click', () => removeRuleCondition(Number(button.dataset.removeConditionIndex)));
  });
}
```

- [x] **Step 9: 实现表单操作函数**

放在 `renderGroupRuleForm()` 后：

```js
function openNewGroupRuleForm() {
  state.ruleEditor.visible = true;
  state.ruleEditor.editingRuleId = '';
  state.ruleEditor.draft = createDefaultGroupRuleDraft();
  renderGroupRuleForm();
}

function closeGroupRuleForm() {
  state.ruleEditor.visible = false;
  state.ruleEditor.editingRuleId = '';
  state.ruleEditor.draft = null;
  renderGroupRuleForm();
}

function updateRuleDraftFromForm() {
  if (!state.ruleEditor.visible) {
    return;
  }

  const thresholdValue = document.getElementById('groupRuleThresholdInput').value;
  const conditions = Array.from(document.querySelectorAll('#groupRuleConditionList .condition-row')).map((row) => {
    return {
      field: row.querySelector('[data-condition-field="field"]').value,
      operator: row.querySelector('[data-condition-field="operator"]').value,
      value: row.querySelector('[data-condition-field="value"]').value
    };
  });

  state.ruleEditor.draft = {
    name: document.getElementById('groupRuleNameInput').value.trim(),
    targetTitle: document.getElementById('groupRuleTargetTitleInput').value.trim(),
    enabled: document.getElementById('groupRuleEnabledInput').checked,
    minTabsPerGroup: thresholdValue ? Number(thresholdValue) : null,
    conditions
  };

  renderGroupRuleForm();
}

function addRuleCondition() {
  updateRuleDraftFromForm();

  if (state.ruleEditor.draft.conditions.length >= 5) {
    setStatus('每条规则最多 5 个条件');
    return;
  }

  state.ruleEditor.draft.conditions.push({
    field: 'hostname',
    operator: 'contains',
    value: ''
  });
  renderGroupRuleForm();
}

function removeRuleCondition(index) {
  updateRuleDraftFromForm();
  state.ruleEditor.draft.conditions = state.ruleEditor.draft.conditions.filter((condition, conditionIndex) => conditionIndex !== index);
  renderGroupRuleForm();
}
```

- [x] **Step 10: 实现保存和规则动作**

放在 `removeRuleCondition()` 后：

```js
function buildRulePayloadFromDraft() {
  updateRuleDraftFromForm();
  const draft = state.ruleEditor.draft;
  const targetTitle = draft.targetTitle.trim();
  const name = draft.name.trim() || targetTitle;

  if (!targetTitle) {
    throw new Error('目标分组名不能为空');
  }

  if (!name) {
    throw new Error('规则名称不能为空');
  }

  if (draft.minTabsPerGroup !== null && (!Number.isInteger(draft.minTabsPerGroup) || draft.minTabsPerGroup < 1)) {
    throw new Error('规则分组阈值必须是不小于 1 的整数，或留空使用全局阈值');
  }

  if (draft.conditions.some((condition) => !condition.value.trim())) {
    throw new Error('匹配内容不能为空');
  }

  return {
    name,
    enabled: draft.enabled,
    targetTitle,
    minTabsPerGroup: draft.minTabsPerGroup,
    conditions: draft.conditions.map((condition) => ({
      field: condition.field,
      operator: condition.operator,
      value: condition.value.trim()
    }))
  };
}

async function saveGroupRule(event) {
  event.preventDefault();
  setBusy(true);

  try {
    const rule = buildRulePayloadFromDraft();
    const action = state.ruleEditor.editingRuleId ? 'update-group-rule' : 'create-group-rule';
    const payload = state.ruleEditor.editingRuleId ? {
      ruleId: state.ruleEditor.editingRuleId,
      rule
    } : {
      rule
    };
    const result = await sendMessage(action, payload);
    state.settings = result.settings || state.settings;
    closeGroupRuleForm();
    setStatus(`已保存分组规则：${rule.name || rule.targetTitle}`);
    await loadState({ keepStatus: true, keepMoreToolsFocus: true });
  } catch (error) {
    setStatus(error.message || '保存分组规则失败');
  } finally {
    setBusy(false);
  }
}

async function handleGroupRuleAction(action, ruleId) {
  const rule = (state.settings.groupRules || []).find((item) => item.id === ruleId);

  if (!rule) {
    setStatus('没有找到分组规则');
    return;
  }

  if (action === 'edit-rule') {
    state.ruleEditor.visible = true;
    state.ruleEditor.editingRuleId = ruleId;
    state.ruleEditor.draft = {
      name: rule.name,
      enabled: rule.enabled,
      targetTitle: rule.targetTitle,
      minTabsPerGroup: rule.minTabsPerGroup,
      conditions: rule.conditions.map((condition) => Object.assign({}, condition))
    };
    renderGroupRuleForm();
    return;
  }

  if (action === 'delete-rule' && !window.confirm('删除后无法恢复，确定删除这条分组规则吗？')) {
    setStatus('已取消删除分组规则');
    return;
  }

  const messageMap = {
    'toggle-rule': ['update-group-rule', { ruleId, rule: { enabled: !rule.enabled } }],
    'move-rule-up': ['move-group-rule', { ruleId, direction: 'up' }],
    'move-rule-down': ['move-group-rule', { ruleId, direction: 'down' }],
    'delete-rule': ['delete-group-rule', { ruleId }]
  };
  const [messageAction, payload] = messageMap[action] || [];

  if (!messageAction) {
    return;
  }

  await runAction(messageAction, payload);
}
```

- [x] **Step 11: 扩展 `formatActionResult`**

在默认返回前增加：

```js
  if (action === 'create-group-rule' || action === 'update-group-rule') {
    return `已保存分组规则：${result.rule.name}`;
  }

  if (action === 'delete-group-rule') {
    return '已删除分组规则';
  }

  if (action === 'move-group-rule') {
    return result.moved ? '已调整分组规则顺序' : '分组规则顺序未变化';
  }
```

- [x] **Step 12: 运行校验**

Run:

```bash
node --check popup.js
node scripts/校验插件.js
```

Expected: 两条命令都成功。

---

### Task 6: 弹窗样式

**Files:**
- Modify: `popup.css`
- Test: `scripts/校验插件.js`

- [x] **Step 1: 写样式存在性校验**

在 `scripts/校验插件.js` 读取 `popupHtml` 后新增读取 CSS：

```js
const popupCssContent = fs.readFileSync(path.join(rootDir, 'popup.css'), 'utf8');
assert.ok(popupCssContent.includes('.group-rule-form'));
assert.ok(popupCssContent.includes('.condition-row'));
assert.ok(popupCssContent.includes('.group-rule-item'));
```

- [x] **Step 2: 运行校验确认失败**

Run: `node scripts/校验插件.js`

Expected: FAIL，错误来自 CSS 断言。

- [x] **Step 3: 追加规则区域样式**

在 `popup.css` 的 `.group-item` 相关样式后追加：

```css
.group-rule-list {
  display: grid;
  gap: 8px;
  max-height: 220px;
  overflow: auto;
}

.group-rule-item {
  display: grid;
  gap: 8px;
  padding: 10px;
  border: 1px solid var(--line);
  border-radius: 8px;
  background: var(--surface);
}

.group-rule-item.is-disabled {
  background: var(--surface-soft);
}

.group-rule-main {
  display: grid;
  gap: 3px;
  min-width: 0;
}

.group-rule-main strong,
.group-rule-main span {
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.group-rule-main strong {
  font-size: 13px;
}

.group-rule-main span {
  color: var(--muted);
  font-size: 12px;
}

.group-rule-actions {
  flex-wrap: wrap;
}

.group-rule-form {
  display: grid;
  gap: 10px;
  margin-bottom: 10px;
  padding: 10px;
  border: 1px solid var(--line);
  border-radius: 8px;
  background: var(--surface-soft);
}

.rule-field {
  display: grid;
  gap: 4px;
  font-size: 12px;
}

.rule-field input,
.condition-row input,
.condition-row select {
  min-width: 0;
  height: 32px;
  border: 1px solid var(--line);
  border-radius: 8px;
  padding: 0 8px;
  background: var(--surface);
  font-size: 12px;
}

.rule-field small {
  color: var(--muted);
  font-size: 11px;
  line-height: 1.35;
}

.rule-toggle {
  display: flex;
  align-items: center;
  gap: 6px;
  color: var(--muted);
  font-size: 12px;
}

.condition-editor {
  display: grid;
  gap: 8px;
}

.condition-editor-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  font-size: 12px;
  font-weight: 600;
}

.condition-list {
  display: grid;
  gap: 6px;
}

.condition-row {
  display: grid;
  grid-template-columns: 76px 76px minmax(0, 1fr) auto;
  gap: 6px;
}
```

- [x] **Step 4: 运行校验**

Run: `node scripts/校验插件.js`

Expected: PASS。

---

### Task 7: README 和完整校验

**Files:**
- Modify: `README.md`
- Test: `scripts/校验插件.js`

- [x] **Step 1: 写 README 文案校验**

在 `scripts/校验插件.js` 的 README 断言附近增加：

```js
assert.ok(readmeContent.includes('自定义分组规则'));
assert.ok(readmeContent.includes('规则阈值'));
assert.ok(readmeContent.includes('留空使用全局阈值'));
```

- [x] **Step 2: 运行校验确认失败**

Run: `node scripts/校验插件.js`

Expected: FAIL，错误来自 README 文案断言。

- [x] **Step 3: 更新 README 分组规则说明**

在 `README.md` 的“分组规则是插件的核心能力”列表后追加：

```markdown
- 可以在“更多工具”里新增自定义分组规则，用域名、主域名、路径、网址或标题匹配标签页。
- 多条规则可以指向同一个自定义分组，适合把代码仓库、文档和任务系统归成同一个项目。
- 规则阈值可以单独设置；留空使用全局阈值，界面会直接显示最终生效的阈值。
- 即使没有达到建组阈值，同一项目下的标签页在整理后也会相邻排列，阈值只决定是否创建 Chrome 原生分组。
```

- [x] **Step 4: 运行最终校验**

Run:

```bash
node --check background.js
node --check popup.js
node scripts/校验插件.js
```

Expected: 三条命令都成功。

- [ ] **Step 5: 手动加载插件检查**

在 Chrome 的 `chrome://extensions` 重新加载插件后检查：

1. 打开弹窗，点击“更多工具”。
2. “分组规则”区域可见。
3. 点击“新增分组规则”，规则表单展开。
4. 分组阈值为空时显示“当前全局：至少 N 个标签”。
5. 条件值为空时保存失败并提示“匹配内容不能为空”。
6. 新增两条规则分别匹配 GitHub 和文档站点，并指向同一个目标分组。
7. 点击“整理当前窗口”，命中的标签即使未达到建组阈值也相邻排列。

Expected: 所有步骤符合预期，后台页面没有未处理异常。

---

## 自检

- 设计文档中的规则模型、默认值、规则级阈值、排序聚拢、后台接口、弹窗交互、错误处理和验证方式均有对应任务。
- 计划只修改当前插件已有文件，没有引入构建步骤或外部依赖。
- 所有新增说明、错误提示和 README 文案均为中文。
- 测试先行覆盖核心纯函数、排序行为、后台消息、弹窗结构、样式入口和 README 文案。
