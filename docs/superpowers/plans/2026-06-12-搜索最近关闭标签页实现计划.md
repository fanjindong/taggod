# 搜索最近关闭标签页实现计划

> **给自动化执行者：** 实施本计划时建议使用 `superpowers:subagent-driven-development`。所有步骤使用复选框跟踪，逐项完成后再进入下一项。

**目标：** 让弹窗在用户输入搜索词后同时搜索已打开标签页和 Chrome 最近关闭标签页，并支持恢复最近关闭结果。

**架构：** 后台继续作为浏览器 API 访问边界，负责读取 `chrome.sessions.getRecentlyClosed()` 并转换为搜索快照。前台只在内存里合并、过滤、排序和渲染结果，空搜索路径不读取最近关闭结果参与排序，避免默认态变慢或语义变化。

**技术栈：** Chrome 扩展 Manifest V3、`chrome.tabs`、`chrome.sessions`、原生 JavaScript、静态 HTML/CSS。

---

## 文件结构

- 修改 `manifest.json`：新增 `sessions` 权限。
- 修改 `background.js`：新增最近关闭快照构建、状态返回字段和恢复动作。
- 修改 `popup.js`：保存最近关闭快照，只在有搜索词时合并搜索，按结果类型执行切换或恢复。
- 修改 `popup.html`：调整搜索标签文案，说明输入后可搜最近关闭。
- 修改 `README.md`：更新搜索能力和权限说明。
- 修改 `scripts/校验插件.js`：同步权限、界面文案和最近关闭快照校验。
- 不新增测试文件：项目当前使用脚本语法检查和插件校验脚本作为本地验证入口，本次直接扩展现有校验脚本。

## 关键约定

- 最近关闭会话请求上限为 `25` 条，原因是 Chrome API 对 `maxResults` 有硬性限制。
- 最近关闭快照上限为 `100` 条，原因是关闭窗口会话可能拆出多个标签，过大列表会拖慢弹窗输入。
- 搜索结果渲染上限为 `100` 条，原因是弹窗 DOM 过多会降低键盘选择和滚动响应。
- 最近关闭结果的 `resultType` 固定为 `recentlyClosed`。
- 已打开结果的 `resultType` 固定为 `open`，旧数据缺省时按 `open` 处理，避免破坏已有逻辑。
- 恢复最近关闭会话使用 `chrome.sessions.restore(sessionId)`，编号来自 `tab.sessionId` 或 `window.sessionId`，遵守 Chrome 原生会话恢复粒度。

### 任务 1：新增权限和常量

**文件：**

- 修改：`manifest.json`
- 修改：`background.js`
- 修改：`popup.js`

- [ ] **步骤 1：在 `manifest.json` 新增 `sessions` 权限**

将权限数组改为：

```json
"permissions": [
  "tabs",
  "tabGroups",
  "storage",
  "sessions"
]
```

- [ ] **步骤 2：在 `background.js` 增加最近关闭上限常量**

在 `RECENT_ACCESS_LIMIT` 附近加入：

```javascript
// Chrome 限制最近关闭会话查询最多 25 条，请求更大数值会直接抛错。
const RECENTLY_CLOSED_SESSION_LIMIT = 25;
// 会话里的关闭窗口可能包含多个标签，拆分后的搜索结果仍要限量，避免弹窗输入时处理过多数据。
const RECENTLY_CLOSED_SEARCH_LIMIT = 100;
```

- [ ] **步骤 3：在 `popup.js` 增加状态字段和渲染上限常量**

在 `state` 中加入：

```javascript
recentlyClosedTabs: [],
```

在 `DEFAULT_RECENT_RESULT_LIMIT` 后加入：

```javascript
// 搜索结果最多渲染 100 条，原因是弹窗空间有限，过多 DOM 会影响键盘选择响应。
const SEARCH_RESULT_LIMIT = 100;
```

### 任务 2：后台构建最近关闭搜索快照

**文件：**

- 修改：`background.js`

- [ ] **步骤 1：新增最近关闭标签快照构建函数**

在 `buildSearchTabSnapshots` 附近新增函数：

```javascript
function buildRecentlyClosedTabSnapshots(recentlyClosedSessions, settings) {
  const snapshots = [];
  const normalizedSettings = normalizeSettings(settings);

  for (const session of Array.isArray(recentlyClosedSessions) ? recentlyClosedSessions : []) {
    if (snapshots.length >= RECENTLY_CLOSED_SEARCH_LIMIT) {
      break;
    }

    const closedAt = Number(session.lastModified) || 0;
    const sessionId = String(session.sessionId || '');

    if (session.tab) {
      snapshots.push(buildRecentlyClosedTabSnapshot(session.tab, {
        closedAt,
        sessionId,
        settings: normalizedSettings,
        sourceIndex: snapshots.length
      }));
      continue;
    }

    const windowTabs = session.window && Array.isArray(session.window.tabs) ? session.window.tabs : [];

    for (const tab of windowTabs) {
      if (snapshots.length >= RECENTLY_CLOSED_SEARCH_LIMIT) {
        break;
      }

      snapshots.push(buildRecentlyClosedTabSnapshot(tab, {
        closedAt,
        sessionId,
        settings: normalizedSettings,
        sourceIndex: snapshots.length
      }));
    }
  }

  return snapshots.filter(Boolean);
}

function buildRecentlyClosedTabSnapshot(tab, options) {
  const safeTab = tab || {};
  const url = safeTab.url || '';
  const groupInfo = getResolvedGroupInfo(safeTab, options.settings);

  return {
    resultType: 'recentlyClosed',
    sessionId: options.sessionId,
    id: `closed-${options.sessionId}-${options.sourceIndex}`,
    title: safeTab.title || '未命名标签',
    url,
    groupKey: groupInfo.groupKey,
    groupTitle: groupInfo.title,
    shortGroupTitle: getShortGroupTitle(groupInfo.groupKey),
    closedAt: options.closedAt,
    lastAccessedAt: options.closedAt,
    isCurrentWindow: false,
    windowLabel: '最近关闭',
    index: options.sourceIndex
  };
}
```

- [ ] **步骤 2：在 `getPopupState` 中读取最近关闭会话**

把 `Promise.all` 扩展为同时读取最近关闭会话：

```javascript
const [tabs, allTabs, recentlyClosedSessions, stored] = await Promise.all([
  queryCurrentWindowTabs(),
  queryAllWindowTabs(),
  chrome.sessions.getRecentlyClosed({ maxResults: RECENTLY_CLOSED_SESSION_LIMIT }).catch(() => []),
  chrome.storage.local.get([
    STORAGE_KEYS.sessions,
    STORAGE_KEYS.settings,
    STORAGE_KEYS.recentAccess
  ])
]);
```

在返回值中加入：

```javascript
recentlyClosedTabs: buildRecentlyClosedTabSnapshots(recentlyClosedSessions, settings),
```

### 任务 3：后台支持恢复最近关闭会话

**文件：**

- 修改：`background.js`

- [ ] **步骤 1：新增恢复函数**

在 `activateTabAcrossWindows` 后新增：

```javascript
async function restoreClosedSession(sessionId) {
  const safeSessionId = String(sessionId || '').trim();

  if (!safeSessionId) {
    throw new Error('历史标签无效');
  }

  try {
    const restoredSession = await chrome.sessions.restore(safeSessionId);

    return {
      restored: true,
      sessionId: safeSessionId,
      tabId: restoredSession && restoredSession.tab && Number.isInteger(restoredSession.tab.id)
        ? restoredSession.tab.id
        : null,
      windowId: restoredSession && restoredSession.window && Number.isInteger(restoredSession.window.id)
        ? restoredSession.window.id
        : null
    };
  } catch (error) {
    // 最近关闭会话可能已经被 Chrome 清理或被新的关闭记录挤出，统一提示能避免暴露浏览器内部错误文案。
    throw new Error('历史标签可能已过期');
  }
}
```

- [ ] **步骤 2：在消息路由中加入新动作**

在 `handleMessage` 的 `activate-tab` 分支附近加入：

```javascript
if (action === 'restore-closed-session') {
  return restoreClosedSession(message.sessionId);
}
```

### 任务 4：前台保存和搜索最近关闭快照

**文件：**

- 修改：`popup.js`

- [ ] **步骤 1：加载状态时保存最近关闭快照**

在 `loadState` 中设置：

```javascript
state.recentlyClosedTabs = data.recentlyClosedTabs || [];
```

- [ ] **步骤 2：让搜索文本函数兼容最近关闭结果**

确认 `getTabSearchText` 使用的字段包含 `title`、`url`、`groupKey`、`groupTitle` 和 `shortGroupTitle`。如果缺少 `shortGroupTitle`，补充：

```javascript
tab.shortGroupTitle || ''
```

- [ ] **步骤 3：修改可见结果计算**

将 `getVisibleTabsFromState` 的有搜索词分支改为：

```javascript
const recentlyClosedTabs = Array.isArray(sourceState.recentlyClosedTabs) ? sourceState.recentlyClosedTabs : [];
const searchPool = [
  ...tabs.map((tab) => Object.assign({ resultType: 'open' }, tab)),
  ...recentlyClosedTabs
];

return searchPool
  .filter((tab) => getSearchMatchScore(tab, query) > 0 || getTabSearchText(tab).includes(query))
  .sort((left, right) => compareSearchTabs(left, right, query))
  .slice(0, SEARCH_RESULT_LIMIT);
```

空搜索分支不使用 `recentlyClosedTabs`。

- [ ] **步骤 4：同分排序时已打开结果优先**

在 `compareSearchTabs` 中，评分比较后、最近使用比较前加入：

```javascript
const leftType = left.resultType || 'open';
const rightType = right.resultType || 'open';

if (leftType !== rightType) {
  return leftType === 'open' ? -1 : 1;
}
```

### 任务 5：前台渲染和执行恢复动作

**文件：**

- 修改：`popup.js`

- [ ] **步骤 1：新增统一结果执行函数**

替换 `activateSearchResult(tabId)` 为：

```javascript
async function openSearchResult(result) {
  if (!result) {
    setStatus('没有可打开的标签页');
    return;
  }

  if (result.resultType === 'recentlyClosed') {
    if (!result.sessionId) {
      setStatus('历史标签无效');
      return;
    }

    await runAction('restore-closed-session', { sessionId: result.sessionId });
    return;
  }

  if (!Number.isInteger(result.id)) {
    setStatus('目标标签无效');
    return;
  }

  await runAction('activate-tab', { tabId: result.id });
}
```

- [ ] **步骤 2：修改回车处理**

把 `handleSearchKeydown` 中的：

```javascript
activateSearchResult(selectedTab.id);
```

改为：

```javascript
openSearchResult(selectedTab);
```

- [ ] **步骤 3：修改结果点击处理**

把 `renderTabs` 中的点击事件改为：

```javascript
item.addEventListener('click', () => openSearchResult(tab));
```

- [ ] **步骤 4：渲染最近关闭标识**

在 `renderTabs` 中生成元信息时按类型分支：

```javascript
const isRecentlyClosed = tab.resultType === 'recentlyClosed';
const accessLabel = formatRecentAccessTime(Number(tab.lastAccessedAt) || 0);
const windowLabel = isRecentlyClosed ? '最近关闭' : (tab.windowLabel || (tab.isCurrentWindow ? '当前窗口' : '其他窗口'));
const metaText = accessLabel ? `${windowLabel} · ${accessLabel}` : windowLabel;
```

- [ ] **步骤 5：调整空状态文案**

将有搜索词但无结果时的文案改为：

```javascript
tabList.appendChild(createEmptyState(state.query ? '没有匹配的标签页' : '当前没有可切换的标签页'));
```

- [ ] **步骤 6：补充动作结果文案**

在 `formatActionResult` 中加入：

```javascript
if (action === 'restore-closed-session') {
  return '已恢复最近关闭标签页';
}
```

### 任务 6：更新界面和 README 文案

**文件：**

- 修改：`popup.html`
- 修改：`README.md`

- [ ] **步骤 1：更新搜索框标签**

将 `popup.html` 中搜索框标签从：

```html
<span>搜索已打开标签</span>
```

改为：

```html
<span>搜索标签页</span>
```

- [ ] **步骤 2：更新搜索框占位文案**

将占位文案改为：

```html
placeholder="输入标题、网址或分组名"
```

如果当前文案已经一致，不需要修改。

- [ ] **步骤 3：更新 README 搜索说明**

把 README 中“可以输入标题、网址、主域名或自定义分组名，在所有已打开窗口中查找标签页”改为：

```markdown
可以输入标题、网址、主域名或自定义分组名，在所有已打开窗口中查找标签页；输入关键词后，也会搜索 Chrome 最近关闭的标签页。未输入时仍只展示最近使用的已打开页面，避免历史结果干扰快速切换。
```

- [ ] **步骤 4：更新 README 权限说明**

在权限列表中加入：

```markdown
- `sessions`：读取和恢复 Chrome 最近关闭的标签页或窗口，用于输入搜索词后找回刚关闭的页面。
```

### 任务 7：本地校验

**文件：**

- 校验：`manifest.json`
- 校验：`background.js`
- 校验：`popup.js`
- 校验：`scripts/校验插件.js`
- 修改：`scripts/校验插件.js`

- [ ] **步骤 1：更新插件校验脚本**

在 `scripts/校验插件.js` 中同步检查 `sessions` 权限、`搜索标签页` 文案、README 最近关闭说明，并新增 `buildRecentlyClosedTabSnapshots` 的基础断言：

```javascript
assert.ok(manifest.permissions.includes('sessions'));
assert.ok(popupStructureHtmlContent.includes('搜索标签页'));
assert.ok(readmeStructureContent.includes('最近关闭的标签页'));
assert.ok(readmeStructureContent.includes('`sessions`'));

const recentlyClosedSnapshots = backgroundSandbox.buildRecentlyClosedTabSnapshots([
  {
    sessionId: 'closed-tab-1',
    lastModified: 1710000000000,
    tab: {
      title: '关闭页面',
      url: 'https://docs.example.com/page'
    }
  }
], {});

assert.strictEqual(recentlyClosedSnapshots.length, 1);
assert.strictEqual(recentlyClosedSnapshots[0].resultType, 'recentlyClosed');
assert.strictEqual(recentlyClosedSnapshots[0].sessionId, 'closed-tab-1');
assert.strictEqual(recentlyClosedSnapshots[0].groupKey, 'example.com');
```

- [ ] **步骤 2：检查后台语法**

运行：

```bash
node --check background.js
```

预期：命令成功，无语法错误。

- [ ] **步骤 3：检查前台语法**

运行：

```bash
node --check popup.js
```

预期：命令成功，无语法错误。

- [ ] **步骤 4：运行插件校验脚本**

运行：

```bash
node scripts/校验插件.js
```

预期：命令成功，输出插件校验通过。

- [ ] **步骤 5：检查新增文档和注释语言**

运行：

```bash
rg -n "// [A-Za-z]|/\\* [A-Za-z]|TODO|TBD" background.js popup.js README.md docs/superpowers/specs/2026-06-12-搜索最近关闭标签页设计文档.md docs/superpowers/plans/2026-06-12-搜索最近关闭标签页实现计划.md
```

预期：没有英文注释、占位符或未说明后续方向的 TODO。

## 自检清单

- 设计中的“只在输入搜索词后搜索最近关闭标签页”由任务 4 覆盖。
- 设计中的 `sessions` 权限由任务 1 和任务 6 覆盖。
- 设计中的最近关闭上限和渲染上限由任务 1、任务 2 和任务 4 覆盖。
- 设计中的恢复动作由任务 3 和任务 5 覆盖。
- 设计中的中文错误提示由任务 3 和任务 5 覆盖。
- 设计中的 README 更新由任务 6 覆盖。
- 本计划没有占位符；`TODO` 只出现在校验命令里，用于检查代码中是否存在未说明后续方向的待办项。
