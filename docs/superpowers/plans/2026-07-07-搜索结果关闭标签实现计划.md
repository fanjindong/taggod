# 搜索结果关闭标签实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在搜索结果中支持关闭单个已打开普通标签页，并保持现有搜索、键盘选择和跳转体验稳定。

**Architecture:** 后台新增一个受保护的关闭消息入口，负责最终安全校验和调用 Chrome Tabs API。前台把搜索结果从单按钮结构调整为“结果容器 + 打开按钮 + 关闭按钮”，并复用同一套搜索结果键盘选择逻辑。关闭成功后前台先本地移除结果并保持焦点在结果列表上下文；第一阶段只保证搜索结果列表和可见结果数量立即更新，概览、重复提示和高级管理摘要允许短暂不一致，原因是完整刷新会默认把焦点带回搜索框。

**Tech Stack:** Chrome Extension Manifest V3、原生 JavaScript、原生 HTML/CSS、`chrome.tabs`、`chrome.windows`、Node.js 校验脚本。

## Global Constraints

- 所有文档、界面文案和代码注释必须使用中文。
- 不新增构建步骤，不引入外部依赖。
- 不修改 `manifest.json` 权限，继续使用当前 `tabs` 权限完成关闭动作。
- 不支持批量关闭，不支持关闭最近关闭结果，不新增 `Delete` 或 `Backspace` 关闭快捷键。
- 固定标签、当前聚焦窗口的当前活动标签、正在播放音频的标签必须受保护。
- 搜索结果不得出现按钮嵌套，打开按钮和关闭按钮必须是兄弟节点。
- 关闭后焦点必须保持在结果列表上下文中。
- 默认不得执行 `git add`、`git commit`、`git push` 或创建 PR；所有改动保留在未暂存状态。

---

## File Structure

- Modify: `background.js`
  - 新增 `closeSearchResultTab(tabId)`。
  - 新增当前聚焦窗口活动标签判断辅助函数。
  - 在 `handleMessage` 中接入 `close-search-result-tab`。
- Modify: `popup.html`
  - 给 `searchResultList` 增加 `tabindex="-1"`，作为空列表焦点兜底。
- Modify: `popup.js`
  - 给搜索结果快照补充 `audible` 字段。
  - 新增可关闭判断、关闭结果、焦点恢复、共享键盘处理函数。
  - 重构 `renderTabs()` 的结果项 DOM，避免按钮嵌套。
- Modify: `popup.css`
  - 调整搜索结果项为根节点容器样式。
  - 新增打开按钮、关闭操作槽、关闭按钮样式。
- Modify: `scripts/校验插件.js`
  - 增加后台关闭动作测试。
  - 增加前台结构、样式、键盘函数的静态校验。
- Modify: `README.md`
  - 增加搜索结果可关闭普通标签页的中文说明。

---

### Task 1: 后台关闭消息与安全校验

**Files:**
- Modify: `scripts/校验插件.js`
- Modify: `background.js`

**Interfaces:**
- Consumes: `chrome.tabs.get(tabId)`、`chrome.tabs.remove(tabId)`、`chrome.windows.getLastFocused()`
- Produces: `async function closeSearchResultTab(tabId)`
- Produces: `handleMessage({ action: 'close-search-result-tab', tabId })`
- Returns on success:

```js
{
  closedTabId: 123,
  title: '页面标题',
  url: 'https://example.com'
}
```

- Throws Chinese errors:
  - `目标标签无效`
  - `关闭失败，标签可能已经不存在`
  - `该标签受保护，未关闭`
  - `关闭失败，请稍后重试`

- [ ] **Step 1: Write the failing backend checks**

在 `scripts/校验插件.js` 的 `runAsyncChecks()` 开头、现有优先分组测试之前加入以下测试代码。这里先直接调用即将新增的 `backgroundSandbox.closeSearchResultTab`，第一次运行应失败，证明测试能捕获缺失实现。

```js
  const closeSearchResultRemovedIds = [];
  const closeSearchResultTabs = new Map([
    [101, { id: 101, title: '普通页面', url: 'https://example.com/a', windowId: 1, active: false, pinned: false, audible: false }],
    [102, { id: 102, title: '固定页面', url: 'https://example.com/pinned', windowId: 1, active: false, pinned: true, audible: false }],
    [103, { id: 103, title: '当前页面', url: 'https://example.com/current', windowId: 1, active: true, pinned: false, audible: false }],
    [104, { id: 104, title: '播放页面', url: 'https://example.com/audio', windowId: 1, active: false, pinned: false, audible: true }],
    [105, { id: 105, title: '其他窗口活动页', url: 'https://example.com/other-window', windowId: 2, active: true, pinned: false, audible: false }]
  ]);

  backgroundSandbox.chrome.windows = {
    getLastFocused: async () => ({ id: 1 })
  };
  backgroundSandbox.chrome.tabs.get = async (tabId) => {
    if (!closeSearchResultTabs.has(tabId)) {
      throw new Error('不存在');
    }

    return closeSearchResultTabs.get(tabId);
  };
  backgroundSandbox.chrome.tabs.remove = async (tabId) => {
    closeSearchResultRemovedIds.push(tabId);
  };

  const closeSearchResult = await backgroundSandbox.closeSearchResultTab(101);
  assert.deepStrictEqual(closeSearchResult, {
    closedTabId: 101,
    title: '普通页面',
    url: 'https://example.com/a'
  });
  assert.deepStrictEqual(closeSearchResultRemovedIds, [101]);

  await assert.rejects(
    () => backgroundSandbox.closeSearchResultTab('101'),
    /目标标签无效/
  );
  await assert.rejects(
    () => backgroundSandbox.closeSearchResultTab(999),
    /关闭失败，标签可能已经不存在/
  );
  await assert.rejects(
    () => backgroundSandbox.closeSearchResultTab(102),
    /该标签受保护，未关闭/
  );
  await assert.rejects(
    () => backgroundSandbox.closeSearchResultTab(103),
    /该标签受保护，未关闭/
  );
  await assert.rejects(
    () => backgroundSandbox.closeSearchResultTab(104),
    /该标签受保护，未关闭/
  );

  const otherWindowActiveResult = await backgroundSandbox.closeSearchResultTab(105);
  assert.strictEqual(otherWindowActiveResult.closedTabId, 105);
  assert.deepStrictEqual(closeSearchResultRemovedIds, [101, 105]);

  const handleCloseResult = await backgroundSandbox.handleMessage({
    action: 'close-search-result-tab',
    tabId: 101
  });
  assert.strictEqual(handleCloseResult.closedTabId, 101);
```

- [ ] **Step 2: Run the failing check**

Run:

```bash
node scripts/校验插件.js
```

Expected: fail with `backgroundSandbox.closeSearchResultTab is not a function`.

- [ ] **Step 3: Add background helpers and close action**

在 `background.js` 的 `closeSavedTabs` 附近新增以下函数。放在后台批量关闭相关逻辑附近，原因是它们都属于标签关闭动作，但这个函数只服务搜索结果单标签关闭。

```js
async function getLastFocusedWindowForCloseGuard() {
  if (!chrome.windows || typeof chrome.windows.getLastFocused !== 'function') {
    return null;
  }

  try {
    return await chrome.windows.getLastFocused();
  } catch (error) {
    // 聚焦窗口只用于识别当前正在看的活动标签，读取失败时不阻断普通标签关闭。
    return null;
  }
}

function isFocusedActiveTab(tab, focusedWindow) {
  return Boolean(
    tab
    && tab.active
    && focusedWindow
    && typeof focusedWindow.id === 'number'
    && tab.windowId === focusedWindow.id
  );
}

async function closeSearchResultTab(tabId) {
  if (!Number.isInteger(tabId)) {
    throw new Error('目标标签无效');
  }

  let tab;

  try {
    tab = await chrome.tabs.get(tabId);
  } catch (error) {
    throw new Error('关闭失败，标签可能已经不存在');
  }

  const focusedWindow = await getLastFocusedWindowForCloseGuard();

  if (tab.pinned || tab.audible || isFocusedActiveTab(tab, focusedWindow)) {
    throw new Error('该标签受保护，未关闭');
  }

  try {
    await chrome.tabs.remove(tabId);
  } catch (error) {
    throw new Error('关闭失败，请稍后重试');
  }

  return {
    closedTabId: tabId,
    title: tab.title || '',
    url: getTabUrl(tab)
  };
}
```

- [ ] **Step 4: Wire the message action**

在 `background.js` 的 `handleMessage(message)` 中，放在 `close-selected-duplicates` 和 `save-workspace` 之间加入：

```js
  if (action === 'close-search-result-tab') {
    return closeSearchResultTab(message.tabId);
  }
```

- [ ] **Step 5: Run backend validation**

Run:

```bash
node scripts/校验插件.js
node --check background.js
```

Expected: both pass.

---

### Task 2: 搜索结果 DOM 结构与样式迁移

**Files:**
- Modify: `scripts/校验插件.js`
- Modify: `popup.html`
- Modify: `popup.js`
- Modify: `popup.css`

**Interfaces:**
- Consumes: existing `renderTabs()`
- Produces: result item DOM structure:

```html
<article class="tab-item quick-result-item">
  <button class="quick-result-open-button" type="button"></button>
  <span class="quick-result-action-slot"></span>
</article>
```

- [ ] **Step 1: Add failing structural checks**

在 `scripts/校验插件.js` 现有弹窗结构断言附近加入：

```js
assert.ok(popupStructureHtmlContent.includes('id="searchResultList" class="tab-list quick-result-list" tabindex="-1"'));
assert.ok(popupStructureJsContent.includes('quick-result-open-button'));
assert.ok(popupStructureJsContent.includes('quick-result-action-slot'));
assert.ok(popupStructureJsContent.includes("document.createElement('article')"));
assert.ok(!popupStructureJsContent.includes("const item = document.createElement('button');\n    item.className = `tab-item quick-result-item"));
assert.ok(popupStructureCssContent.includes('.quick-result-open-button'));
assert.ok(popupStructureCssContent.includes('.quick-result-action-slot'));
assert.ok(popupStructureCssContent.includes('grid-template-columns: minmax(0, 1fr) 40px;'));
```

- [ ] **Step 2: Run the failing check**

Run:

```bash
node scripts/校验插件.js
```

Expected: fail on the missing `tabindex="-1"` or missing `quick-result-open-button`.

- [ ] **Step 3: Update the result list focus fallback**

在 `popup.html` 中把：

```html
<div id="searchResultList" class="tab-list quick-result-list"></div>
```

改为：

```html
<div id="searchResultList" class="tab-list quick-result-list" tabindex="-1"></div>
```

- [ ] **Step 4: Refactor `renderTabs()` item structure**

在 `popup.js` 的 `renderTabs()` 中，把每条结果从单个按钮改为 `article` 根节点。第一步只做结构，不接入关闭动作。

```js
    const item = document.createElement('article');
    item.className = `tab-item quick-result-item${index === selectedIndex ? ' is-selected' : ''}`;
    item.dataset.action = tab.resultType === 'recentlyClosed' ? 'restore' : 'activate';
    item.dataset.tabId = String(tab.id);

    const openButton = document.createElement('button');
    openButton.className = 'quick-result-open-button';
    openButton.type = 'button';
    openButton.innerHTML = `
      <span class="quick-result-main">
        <span class="tab-title" title="${escapeHtml(tab.title)}">${escapeHtml(tab.title)}</span>
        <span class="tab-url" title="${escapeHtml(tab.groupKey)} · ${escapeHtml(tab.url)}">${escapeHtml(groupTitle)} · ${escapeHtml(tab.url)}</span>
      </span>
      <span class="quick-result-meta">${escapeHtml(metaText)}</span>
    `;
    openButton.addEventListener('click', () => openSearchResult(tab));

    const actionSlot = document.createElement('span');
    actionSlot.className = 'quick-result-action-slot';

    item.appendChild(openButton);
    item.appendChild(actionSlot);
    tabList.appendChild(item);
```

- [ ] **Step 5: Update CSS for the split structure**

在 `popup.css` 的 `.quick-result-item` 附近替换或补充以下样式：

```css
.quick-result-item {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 40px;
  align-items: center;
  column-gap: 8px;
  min-height: 58px;
  padding: 8px 8px 8px 12px;
  transition: border-color 80ms ease, background-color 80ms ease;
}

.quick-result-open-button {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: center;
  column-gap: 8px;
  min-width: 0;
  width: 100%;
  border: 0;
  padding: 2px 0;
  background: transparent;
  color: inherit;
  text-align: left;
}

.quick-result-open-button:hover {
  border-color: transparent;
  background: transparent;
}

.quick-result-action-slot {
  display: grid;
  place-items: center;
  width: 40px;
  height: 40px;
}
```

- [ ] **Step 6: Run structural validation**

Run:

```bash
node scripts/校验插件.js
node --check popup.js
```

Expected: both pass.

---

### Task 3: 前台关闭按钮、状态更新与焦点保持

**Files:**
- Modify: `scripts/校验插件.js`
- Modify: `popup.js`
- Modify: `popup.css`

**Interfaces:**
- Consumes: `sendMessage('close-search-result-tab', { tabId })`
- Produces: search result snapshot field `audible: Boolean(tab.audible)`
- Produces: `canCloseSearchResultTab(tab)`
- Produces: `getSearchResultDisplayName(tab)`
- Produces: `closeSearchResultTab(tab, event)`
- Produces: `focusSelectedSearchResult()`

- [ ] **Step 1: Add failing frontend checks**

在 `scripts/校验插件.js` 的弹窗静态断言附近加入：

```js
assert.ok(popupStructureJsContent.includes('function canCloseSearchResultTab'));
assert.ok(popupStructureJsContent.includes('function getSearchResultDisplayName'));
assert.ok(popupStructureJsContent.includes('async function closeSearchResultTab'));
assert.ok(popupStructureJsContent.includes('function focusSelectedSearchResult'));
assert.ok(popupStructureJsContent.includes("sendMessage('close-search-result-tab'"));
assert.ok(popupStructureJsContent.includes('tab.isCurrentWindow && tab.active'));
assert.ok(popupStructureJsContent.includes('audible: Boolean(tab.audible)'));
assert.ok(popupStructureJsContent.includes('event.stopPropagation()'));
assert.ok(popupStructureJsContent.includes('quick-result-close-button'));
assert.ok(popupStructureCssContent.includes('.quick-result-close-button'));
assert.ok(popupStructureCssContent.includes('.quick-result-close-button:focus-visible'));
```

- [ ] **Step 2: Run the failing check**

Run:

```bash
node scripts/校验插件.js
```

Expected: fail on missing `canCloseSearchResultTab`.

- [ ] **Step 3: Add audible snapshot field**

在 `popup.js` 的 `buildPopupTabSnapshots(tabs, context = {})` 中，把返回对象里的 `pinned` 附近字段改成：

```js
      active: Boolean(tab.active),
      pinned: Boolean(tab.pinned),
      audible: Boolean(tab.audible),
      index: Number.isInteger(tab.index) ? tab.index : 0,
```

这里必须把 Chrome 标签页的播放状态放进前台快照，原因是前台需要直接隐藏正在播放声音标签的关闭入口，不能只依赖后台拒绝。

- [ ] **Step 4: Add close eligibility and display helpers**

在 `popup.js` 的搜索辅助函数区域、`getVisibleTabs()` 附近加入：

```js
function canCloseSearchResultTab(tab) {
  if (!tab) {
    return false;
  }

  const resultType = tab && tab.resultType ? tab.resultType : 'open';

  return resultType === 'open'
    && Number.isInteger(tab.id)
    && tab.pinned !== true
    && !(tab.isCurrentWindow && tab.active)
    && tab.audible !== true;
}

function getSearchResultDisplayName(tab) {
  if (tab && tab.title) {
    return tab.title;
  }

  if (tab && tab.url) {
    return tab.url;
  }

  return '未命名标签页';
}
```

- [ ] **Step 5: Add focus helper**

在 `keepSelectedResultVisible(tabList)` 后面加入：

```js
function focusSelectedSearchResult() {
  const tabList = document.getElementById('searchResultList');
  const selectedOpenButton = tabList.querySelector('.quick-result-item.is-selected .quick-result-open-button');

  if (selectedOpenButton && typeof selectedOpenButton.focus === 'function') {
    selectedOpenButton.focus({ preventScroll: true });
    return;
  }

  if (tabList && typeof tabList.focus === 'function') {
    tabList.focus({ preventScroll: true });
  }
}
```

- [ ] **Step 6: Add close handler**

在 `openSearchResult(result)` 后面加入：

```js
async function closeSearchResultTab(tab, event) {
  if (event && typeof event.stopPropagation === 'function') {
    event.stopPropagation();
  }

  if (!canCloseSearchResultTab(tab)) {
    setStatus('该标签受保护，未关闭');
    focusSelectedSearchResult();
    return;
  }

  try {
    const result = await sendMessage('close-search-result-tab', { tabId: tab.id });
    const closedTabId = Number.isInteger(result.closedTabId) ? result.closedTabId : tab.id;
    const displayName = result.title || getSearchResultDisplayName(tab);

    state.tabs = state.tabs.filter((item) => item.id !== closedTabId);
    state.selectedIndex = clampSelectedIndex(state.selectedIndex, getVisibleTabsFromState(state).length);
    renderTabs();
    setStatus(`已关闭：${displayName}`);
    focusSelectedSearchResult();
  } catch (error) {
    setStatus(error && error.message ? error.message : '关闭失败，请稍后重试');
    focusSelectedSearchResult();
  }
}
```

- [ ] **Step 7: Render the close button**

在 `renderTabs()` 创建 `actionSlot` 后加入：

```js
    if (canCloseSearchResultTab(tab)) {
      const closeButton = document.createElement('button');
      const displayName = getSearchResultDisplayName(tab);

      closeButton.className = 'quick-result-close-button';
      closeButton.type = 'button';
      closeButton.textContent = '×';
      closeButton.title = '关闭标签';
      closeButton.setAttribute('aria-label', `关闭标签：${displayName}`);
      closeButton.addEventListener('click', (event) => closeSearchResultTab(tab, event));
      actionSlot.appendChild(closeButton);
    }
```

- [ ] **Step 8: Add close button styles**

在 `popup.css` 的搜索结果样式附近加入：

```css
.quick-result-close-button {
  width: 32px;
  height: 32px;
  border-color: transparent;
  border-radius: 6px;
  padding: 0;
  background: transparent;
  color: var(--muted);
  font-size: 18px;
  line-height: 1;
  opacity: 0.3;
  transition: opacity 100ms ease, background-color 100ms ease, border-color 100ms ease, color 100ms ease;
}

.quick-result-item:hover .quick-result-close-button,
.quick-result-item:focus-within .quick-result-close-button,
.quick-result-item.is-selected .quick-result-close-button {
  opacity: 1;
}

.quick-result-close-button:hover,
.quick-result-close-button:focus-visible {
  border-color: rgba(220, 38, 38, 0.24);
  background: rgba(254, 226, 226, 0.72);
  color: #b91c1c;
}
```

- [ ] **Step 9: Run frontend validation**

Run:

```bash
node scripts/校验插件.js
node --check popup.js
```

Expected: both pass.

---

### Task 4: 共享搜索结果键盘处理

**Files:**
- Modify: `scripts/校验插件.js`
- Modify: `popup.js`

**Interfaces:**
- Consumes: existing `handleSearchKeydown(event)`
- Produces: `handleSearchResultNavigationKeydown(event)`
- Produces: `handleSearchResultListKeydown(event)`

- [ ] **Step 1: Add failing keyboard checks**

在 `scripts/校验插件.js` 的弹窗静态断言附近加入：

```js
assert.ok(popupStructureJsContent.includes('function handleSearchResultNavigationKeydown'));
assert.ok(popupStructureJsContent.includes('function handleSearchResultListKeydown'));
assert.ok(popupStructureJsContent.includes("document.getElementById('searchResultList').addEventListener('keydown', handleSearchResultListKeydown)"));
assert.ok(!popupStructureJsContent.includes("openButton.addEventListener('keydown', handleSearchResultListKeydown)"));
assert.ok(popupStructureJsContent.includes('handleSearchResultNavigationKeydown(event)'));
```

- [ ] **Step 2: Run the failing check**

Run:

```bash
node scripts/校验插件.js
```

Expected: fail on missing `handleSearchResultNavigationKeydown`.

- [ ] **Step 3: Extract shared navigation logic**

把 `handleSearchKeydown(event)` 中的方向键和回车逻辑抽出为共享函数：

```js
function handleSearchResultNavigationKeydown(event) {
  const visibleTabs = getVisibleTabs();

  if (event.key === 'ArrowDown') {
    event.preventDefault();
    state.selectedIndex = clampSelectedIndex(state.selectedIndex + 1, visibleTabs.length);
    renderTabs();
    return true;
  }

  if (event.key === 'ArrowUp') {
    event.preventDefault();
    state.selectedIndex = clampSelectedIndex(state.selectedIndex - 1, visibleTabs.length);
    renderTabs();
    return true;
  }

  if (event.key === 'Enter') {
    event.preventDefault();
    const selectedTab = visibleTabs[clampSelectedIndex(state.selectedIndex, visibleTabs.length)];

    if (!selectedTab) {
      setStatus('没有可切换的标签页');
      return true;
    }

    openSearchResult(selectedTab);
    return true;
  }

  return false;
}

function handleSearchKeydown(event) {
  handleSearchResultNavigationKeydown(event);
}
```

- [ ] **Step 4: Add list keyboard handler**

在 `handleSearchKeydown(event)` 后加入：

```js
function handleSearchResultListKeydown(event) {
  if (event.target && event.target.closest && event.target.closest('.quick-result-close-button')) {
    return;
  }

  handleSearchResultNavigationKeydown(event);
}
```

- [ ] **Step 5: Bind list keyboard event**

在 `bindEvents()` 中，`searchInput` 的事件绑定后加入：

```js
  document.getElementById('searchResultList').addEventListener('keydown', handleSearchResultListKeydown);
```

不要给 `openButton` 单独绑定 `keydown`，原因是按键事件会从打开按钮冒泡到 `searchResultList`。如果两个节点都绑定同一个处理函数，方向键会一次移动两项，回车也可能重复触发打开。

- [ ] **Step 6: Run keyboard validation**

Run:

```bash
node scripts/校验插件.js
node --check popup.js
```

Expected: both pass.

---

### Task 5: README 与人工验收清单

**Files:**
- Modify: `README.md`
- Modify: `scripts/校验插件.js`

**Interfaces:**
- Consumes: existing README “一键理顺满屏标签” section
- Produces: user-facing Chinese documentation for search-result close action

- [ ] **Step 1: Add failing README check**

在 `scripts/校验插件.js` 的 README 断言附近加入：

```js
assert.ok(readmeContent.includes('搜索结果里的普通标签页可以直接关闭'));
assert.ok(readmeContent.includes('固定标签、当前正在看的标签和正在播放声音的标签不会显示关闭入口'));
```

- [ ] **Step 2: Run the failing check**

Run:

```bash
node scripts/校验插件.js
```

Expected: fail on missing README text.

- [ ] **Step 3: Update README**

在 `README.md` 的“搜索结果支持键盘操作”列表后增加：

```markdown
搜索结果里的普通标签页可以直接关闭。为了避免误关，固定标签、当前正在看的标签和正在播放声音的标签不会显示关闭入口。
```

- [ ] **Step 4: Run full local validation**

Run:

```bash
node scripts/校验插件.js
node --check background.js
node --check popup.js
```

Expected:

```text
插件文件校验通过
```

`node --check` commands should produce no output and exit with code `0`.

- [ ] **Step 5: Manual acceptance checklist**

在 Chrome 扩展弹窗中人工验证以下场景：

```text
1. 搜索普通已打开标签页，结果项右侧显示关闭按钮。
2. 点击结果主体仍然跳转到该标签页。
3. 点击关闭按钮不会跳转，只关闭该标签页。
4. 关闭后结果列表立即移除该项，焦点移动到新的选中结果打开按钮。
5. 列表为空时焦点停留在搜索结果列表容器。
6. 焦点在结果打开按钮上时，↑ / ↓ 仍能移动选中项，Enter 仍能打开选中结果。
7. 焦点在结果打开按钮上时，按 Enter 只打开一次目标标签，不重复触发跳转。
8. 焦点在关闭按钮上时，Enter 只关闭标签，不打开标签。
9. 固定标签不显示关闭按钮。
10. 当前聚焦窗口的当前活动标签不显示关闭按钮。
11. 其他窗口的活动标签如果不是当前聚焦窗口活动标签，可以显示关闭按钮。
12. 正在播放声音的标签不显示关闭按钮。
13. 最近关闭结果不显示关闭按钮。
14. 关闭按钮显隐不挤压标题、网址或元信息。
15. 关闭按钮默认不使用强危险色，hover 和 focus-visible 时危险操作反馈清晰。
```

---

## Self-Review

**Spec coverage:**  
本计划覆盖后台关闭消息、安全边界、前台 DOM 结构、固定操作槽、关闭按钮视觉状态、状态更新、焦点保持、键盘复用、README 和校验脚本。

**Placeholder scan:**  
计划中没有未定义步骤、模糊任务或留给执行者自行补全的描述。

**Type consistency:**  
后台动作名统一为 `close-search-result-tab`；前台关闭函数名为 `closeSearchResultTab(tab, event)`；后台关闭函数名为 `closeSearchResultTab(tabId)`；可关闭判断统一使用 `tab.isCurrentWindow && tab.active` 保护当前聚焦窗口的当前活动标签。
